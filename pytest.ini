[pytest]
# server/ — отдельный набор со своим server/pytest.ini (запуск: cd server && pytest).
# В одном процессе с тестами бота server/main.py перекрывает main.py бота в sys.modules.
norecursedirs = server .* dist build __pycache__ MEMORY_BACKUP scout_sessions
