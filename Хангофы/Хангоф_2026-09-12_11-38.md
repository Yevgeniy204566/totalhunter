# Хангоф 2026-09-12 11:38 — Сессия #140

## Что сделано

Версия на момент сессии: **1.9.2** (без изменений — код-фиксов не делали).

1. **Проверка сервера GCP (по запросу владельца, вне обычного End-of-Session Check).**
   🟢 всё в норме: `totalhunter.service` active непрерывно с 2026-09-02, load average
   0.25, RAM 533Mi available из 969Mi, диск 60% (3.7G свободно), БД 38MB, ответы API
   стабильно ~0.3с. За 30 дней логов — ровно один `StaleDataError` на `/check_auth`
   (2026-09-10 08:18, гонка при одновременном UPDATE строки `users`), не повторялся,
   не критично.

2. **Разобран инцидент владельца с калибровкой (вчера, во время записи видео).**
   Точка А (центр миникарты) была визуально верно выставлена, но биржи после калибровки
   пошли не туда; закрыл/открыл бота — заработало верно тем же сохранённым профилем.
   - Сначала проверил и **исключил** гипотезу «калибровка требует рестарта» —
     `coord_manager.calibrate()` применяется живьём сразу при калибровке (main.py:4894,
     :4910), не только при Save.
   - Затем нашёл **реальный, воспроизводимый баг** в `calibration_ui.py`: клик в окне-лупе
     (`on_canvas_click`) считает смещение от текущего центра и сразу прибавляет его к
     точке, но картинка перерисовывается только по таймеру раз в 150мс — не сразу после
     клика. У владельца мышь Logitech с макро-кнопкой «3 клика за 1 нажатие» — если все
     3 клика прилетают в одно и то же место быстрее 150мс, смещение применяется 3 раза
     подряд без перецентровки → перелёт точки примерно втрое. Задевает только 2 точки
     калибровки (клик-в-лупе механизм), не 7 точек тюнинга (там дискретный D-Pad, точки
     фиксируются по одной).
   - Владелец решил **не чинить** — ускорение перерисовки рискует нагрузить ПК ещё
     сильнее или что бот начнёт тормозить. Согласовано: оставить как есть.
   - Отдельно проверил (по прямой просьбе владельца) **всю процедуру сохранения/загрузки**
     калибровки и тюнинга (`coord_manager.save/load`, `_save_crypt_settings`,
     `_load_crypt_from_profile`) — подтвердил построчно, что это честный round-trip:
     `point_a`/`point_b` сохраняются и загружаются без потерь, `scale_x/y` при загрузке
     не читаются из файла, а пересчитываются той же формулой — расхождений в принципе
     быть не может. Порядок записи (`coord_manager.save()` → `_save_crypt_settings()`
     дочитывает тот же файл и мёржит поверх) безопасен, калибровку не затирает.
   - **Вывод: инцидент закрыт.** Раз сохранённые/загруженные числа математически
     идентичны, дело не в файле профиля — на первом прогоне сыграло что-то временное
     в самом процессе (зависший клик/поток на фоне перегруженного ПК: игра + браузер +
     запись экрана + параллельный Python-процесс другого проекта), а не испорченные
     данные калибровки. Перезапуск сбросил это временное состояние; сама калибровка,
     судя по коду, была верна всё это время. Фикс не нужен, ничего не меняли.

3. **Побочная находка при разборе жалобы «настройки не сохранились» (от знакомого
   владельца) — вероятный вклад в воронку регистраций.** В `ANTI-PATTERNS.md` уже
   задокументирован `AP-TUNE-AUTOSAVE`: слайдеры Бирж/Склепов сохраняются в профиль
   мгновенно, а click-тюнинг (7 точек D-Pad) — **только по явному клику «💾 Сохранить»**,
   так и задумано (чтобы не плодить лишние записи на каждый клик стрелкой). Гипотеза:
   пользователь тюнит 7 точек, по аналогии со слайдерами ждёт, что сохранится само,
   закрывает бот без «Сохранить» — весь тюнинг теряется. Это НЕ новый баг, а известное
   архитектурное решение — но возможно стоит пересмотреть UX (индикатор несохранённых
   изменений или автосохранение). Подробности → `MEMORY/project_onboarding_funnel_diagnosis_sep10.md`.
   **Не согласовано, фикс не делали.**

4. Подготовлено название/описание/теги/хэштеги видео калибровки+тюнинга на английском
   (для иностранной аудитории) — YouTube (title, description, tags, hashtags) и
   RU+EN посты для Discord/Telegram. По ходу владелец попросил уточнить формулировку
   пункта 4 (Crypt Selection in List) — исправлено: это клик нейросети (YOLO), а не
   фиксированная точка, тюнить нужно только если нейросеть промахивается.

5. **Discord-сервер Total Hunter — аудит и точечный фикс прав доступа.** Владелец
   попросил навести порядок (жаловался, что в канале `#основной` пишут все подряд, там
   же публикуются его посты). Сделан read-only аудит через `claude-in-chrome`
   (сервер `Total Hunter`, 10 участников): категория **INFO** (rules/announcements/
   download) уже была правильно настроена — права синхронизированы, `@everyone` не
   может писать; категория **COMMUNITY** (bugs-and-errors/general-chat/screenshots/
   suggestions) открыта для всех — тоже верно. Единственная проблема — `#основной`
   стоял отдельно, вне категорий, без переопределения прав (наследовал открытый
   дефолт сервера). Встроенный Discord Automod не настроен вообще (0 правил) —
   антиспам целиком держится на Carl-bot, который не трогали. Полная реструктуризация
   по расширенному плану (Forum-каналы, Staff-категория, Onboarding, ревизия Carl-bot)
   — владелец сознательно отклонил как избыточную для реальной проблемы, попросил
   только точечный фикс. **Применено** (после явного «да» через AskUserQuestion):
   `@everyone` → «Отправлять сообщения» = ✗ на `#основной`, той же настройкой, что уже
   стоит на `#announcements`. Проверено визуально — применилось, у владельца как у
   владельца сервера поле ввода по-прежнему доступно (ожидаемо).

6. **Сайт total-hunter.com — добавлено новое видео, убран блок отзывов.**
   `web/src/pages/LandingPage.jsx`: блок «Видео-инструкции» перенесён из самого низа
   страницы (был после FAQ) сразу под секцию Features («What Total Hunter Solves»).
   Новое видео калибровки (`27qsJl3hAw8`) добавлено первым в `VIDEO_GUIDES`, старое
   видео про сундуки (`nM_4TzwgrlM`) — вторым, как попросил владелец. Полностью
   удалён блок «We pay for honesty» / «Мы платим за честность» (CTA на 300 алмазов
   за первый отзыв в Telegram) — владелец счёл его лишним. Закоммичено (`c6d555b`),
   задеплоено на Vercel (git push → deploy hook → poll READY → alias total-hunter.com).
   Деплой READY подтверждён напрямую через Vercel API (curl) — PowerShell к API
   Vercel в этот раз не достучался («Невозможно соединиться с удалённым сервером»,
   30 попыток подряд), похоже на тот же класс проблем с ClearVPN, что раньше ломал
   `gcloud compute ssh` (см. `MEMORY/feedback_gcloud_ssh_broken.md`) — на этот раз
   обошлись curl из Bash без каких-либо проблем, само API Vercel было доступно.

## Что делать в следующей сессии

**Открыто, без изменений с сессии #138 — склепы, нехватка масла:**
- Владелец попросил всех знакомых, кто использует бота, понаблюдать за поведением при
  нехватке масла. **На сейчас (2026-09-12) — зависаний не было, проблема пока не
  воспроизвелась живьём.** Три кандидата на фикс всё ещё НЕ согласованы: (а) лимит
  попыток + вернуть лог в `_scroll_and_find()`, (б) детект диалога «недостаточно масла»
  через template-matching (не OCR — сознательно отключён раньше), (в) общий watchdog
  «нет прогресса N минут → мягкая остановка». Ждём живого повтора или явного «да»
  владельца на один из вариантов вслепую.

**Открыто с сессии #139 — воронка регистраций:**
- Фикс двойного трансформа координат Chrome (`0f77c02`) вошёл в 1.9.2 два дня назад —
  рано судить, улучшилась ли конверсия. Сравнить долю «0 попыток» среди новых
  1.9.2-регистраций vs старых версий, когда наберётся статистика.
- Группа «вообще 0 попыток, не про масло» — причина не подтверждена логами. Опционально:
  списаться с 3 юзерами (адреса в `MEMORY/project_onboarding_funnel_diagnosis_sep10.md`).

**Новое с этой сессии:**
- `AP-TUNE-AUTOSAVE` UX-несовпадение (слайдеры автосохраняются, click-тюнинг — нет) —
  возможный вклад в «настройки не сохранились». Не согласовано, обсудить с владельцем,
  нужно ли менять (индикатор несохранённых изменений vs автосохранение).

**Технический долг (не срочно, без изменений с #139):**
- Untracked мусор в `C:\BattleBot` (скриншоты, `_dbg_*.json`, билд-логи, `MEMORY_BACKUP/`,
  `.codex/`, `AGENTS.md`) — разобрать отдельной сессией.

**Закрыто в этой сессии (не переносить дальше):**
- ~~Калибровка Точки А «требует перезапуска»~~ — разобрано полностью, причина найдена
  (тройной клик мыши + перегрузка ПК), фикс не нужен, процедура сохранения проверена
  и подтверждена корректной.
- ~~Discord `#основной` — пишут все подряд~~ — точечный фикс применён и проверен
  (`@everyone`: «Отправлять сообщения» = ✗), остальная структура сервера уже была
  в порядке, полную реструктуризацию владелец отклонил.
- ~~Сайт — добавить видео калибровки, убрать блок отзывов~~ — сделано, закоммичено
  (`c6d555b`), задеплоено, alias total-hunter.com подтверждён на новом деплое.

## GCP End-of-Session Check

🟢 всё в норме — RAM 533Mi/969Mi available, диск 60% (3.7G свободно), load 0.25/0.07/0.02,
`totalhunter.service` active непрерывно с 2026-09-02, БД 38MB. Один разовый `StaleDataError`
за 30 дней (`/check_auth`, 2026-09-10), не повторялся. Очистка не требовалась.
