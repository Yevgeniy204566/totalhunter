# Хангоф 2026-09-20 21:37 (сессия #146, продолжение)

> Файл открыт в начале второй половины сессии (после перерыва пользователя до ~23:00). Раздел
> «Что делать в следующей сессии» дописывается под закрытие — если сессия продолжится сегодня же,
> это будет не «следующая сессия», а продолжение этой же; раздел уточнится по факту.

## Что сделано (первая половина сессии, до перерыва)

1. **План публикации РОЙ (Биржа 2.0 → сайт) — реализован и задеплоен в прод целиком.**
   Task 1-6 (модель, `POST`/`GET /roy/scout-find(s)`, клиент `RoyClient.report_scout_find`, блок
   на сайте, деплой) — все закрыты, по TDD, с прогонами тестов на каждом шаге. Сервер и сайт
   в бою: `GET /roy/scout-finds` отвечает `{"finds":[]}`, `total-hunter.com/roy` — 200.
   - Миграция `roy_scout_finds` накатана через `alembic upgrade head` (не ручной `psql` —
     тот заблокирован auto-mode классификатором Claude Code, найден рабочий обходной путь через
     официальный инструмент миграций, задокументирован в `CLAUDE.md` § «Миграции Alembic на GCP»).
   - Попутно найден и исправлен устаревший тест `test_web_routes.py::test_link_generate_creates_code`
     (проверял старое поведение 10-мин кода привязки, код давно намеренно изменён на 3650 дней).
2. **Калибровка PLAN-A (единый реестр 12 целей вкладки Калибровки) — полный цикл спека→план,
   Final Gate PASS, владелец дал явное «да» на старт кодирования.**
   - Файлы `docs/superpowers/plans/exchange-scout-calibration-plan-a-*-{38,39,40,41,42,43}.md` —
     Stage 0 (Scope) → 1 (Context) → 2 (Archaeology) → 3 (Contracts, PC-01..PC-07) → 4 (Design) →
     5 (Adversarial) → 6 (Test Matrix) → 7 (Self-Audit) → 9 (Final Gate + SPEC→PLAN check).
   - 15+ раундов ревью владельца, каждый находил реальные проблемы: самопротиворечие `tune_card`
     (Design), недоказанный anti-cache тест PC-05, дыра в state machine (видимость `tune_card`
     при переходе offset→absolute), рассинхрон имён тестов с уже названными в
     `docs/superpowers/specs/exchange-scout-calibration-tests-gate-37.md` (T-01..T-13) — все
     закрыты в этой же сессии, не отложены.
   - По пути разобрана и явно припаркована в память отдельная идея — пользовательская калибровка
     `PositionReader.crop_box` (`project_positionreader_user_calibration_scope.md`) — НЕ включена
     в PLAN-A, решено как вход для будущего Part B Design.
   - Отдельно распознан и остановлен реальный context-mix-up: сообщение про «Scout Priority»
     (приоритет показа находок РОЙ по активности пользователя) относилось к другой ветке
     разговора — вовремя пойман, не смешан с текущей работой, идея припаркована отдельно
     (`project_scout_priority_idea_parked.md`).
3. **Начато кодирование Калибровки PLAN-A (TDD, один коммит на Task):**
   - **Task 1 — закрыт, коммит `ed21d9f`.** `CALIBRATION_TARGETS` — реестр 12 записей в `main.py`
     (модуль-уровня, рядом с `TUNE_TARGET_NAMES`). Реализует PC-01 (структура/обязательные поля)
     и PC-02 (двусторонняя проверка с `coord_manager._UI_BUTTON_NAMES`). Тесты —
     `test_calibration_registry.py`, 7/7 зелёных. Старые структуры (`TUNE_TARGET_NAMES` и т. п.)
     пока не удалены — удалятся атомарно вместе с Task 2 (PC-04), не раньше.
   - **Task 2 — в работе, код ещё НЕ начат** (только чтение/анализ `main.py:4772-5260`, вкладка
     калибровки, целиком). Следующий шаг — заменить `CTkOptionMenu`-дропдаун на список из 12
     строк, убрать `_cal_manual_btn`, ввести identity-по-id (PC-03), состояние видимости
     `tune_card` (3 состояния), диспетчеризацию по `kind` (PC-06).
   - **Находка при реализации (не решение владельца, поймано личной проверкой кода при чтении
     `main.py`/`calibration_ui.py`):** план (PC-06, файлы 39/40) утверждал, что `ref_a`/`ref_b`
     «переходят с `calibration_ui._show_red_dot` на `_show_crosshair_at`» — это неточность.
     `_show_red_dot` вызывается ТОЛЬКО внутри самого магнифайера (`calibration_ui.py:141`, во
     время live-перетаскивания курсора мышью), `main.py` его никогда не вызывал и не вызывает —
     мигрировать с одного пути на другой в принципе нечего. Для `ref_a`/`ref_b` в новом списке
     крестик (`_show_crosshair_at`) вообще не нужен: клик по строке сразу запускает `_calibrate()`
     (существующий магнифайер), отдельного статичного превью позиции для абсолютных точек в
     проекте не было и не появится. Учитывается при реализации Task 2 без отдельного раунда
     ревью плана — это уточнение факта о существующем коде, не архитектурное решение.
   - **Важное структурное ограничение, обнаруженное при подготовке Task 2:** в проекте НЕТ ни
     одного теста, который инстанцирует `TotalHunterApp` — в `ANTI-PATTERNS.md` прямой
     анти-паттерн («запуск `main.py` субагентом для проверки пингует продакшен-сервер»). Значит
     живое поведение новых виджетов (клик по строке списка, D-Pad, смена картинки, магнифайер)
     нельзя проверить автотестом в этой сессии — только вручную владельцем в игре (тот же паттерн,
     что ручная проверка PT-13 в плане РОЙ). Автотестами покрывается только чистая логика без
     реального Tk-виджета (реестр, диспетчеризация по данным) — как уже сделано в Task 1.

## Что сделано (вторая половина сессии, после перерыва в 23:00)

4. **Task 2 Калибровки — доделан целиком (4 коммита, TDD):**
   - `64f2f94` — чистые функции без Tk: `cal_target_by_id` (PC-03), `cal_tune_card_visible`
     (Design 4.4, 3 состояния), `cal_visual_dispatch_for_kind` (PC-06),
     `cal_resolve_point_position` (PC-05, anti-cache со счётчиком вызовов). 15 тестов.
   - `e0f8755` — сам виджет: `_target_list_frame` (12 строк) заменяет `CTkOptionMenu`+
     `_cal_manual_btn`; `change_lang()` переписан на identity-по-id. PC-04 атомарно:
     `TUNE_TARGET_NAMES`/`_TUNE_POINT_TARGETS`/`_CHEST_TUNE_RECTS`/`tune_menu_labels`/
     `tune_chest_group_entry_indices`/`_tune_option_menu`/... удалены в этом же коммите;
     `test_tune_menu_labels.py` удалён; новый grep-тест `test_no_legacy_tune_structures_remain`.
     Найдены и НЕ тронуты 10 предсуществующих падений (`test_credit_guards.py` и др.,
     `HuntEngine._bg_threads`, `engine.py:351`) — подтверждено через `git stash`, не связано
     с этим PLAN.
   - `f2b21a6` — закрытие находки Self-Audit (файл 42): анти-паттерны «0 не теряется»/«нет
     autosave» не имели теста — вынесена `cal_apply_offset_delta`, 4 теста.
   - `4429b97` — Task 3, PC-07 (видео-ссылка): `CALIBRATION_VIDEO_URL=""`,
     `cal_video_link_click_url()`, новый LANGS-ключ `cal_video_link` в 19 языках.
   - Итог: **32/32 теста калибровки зелёные**, широкая регрессия (74) чистая.
5. **Первая живая проверка владельцем в игре — нашла реальный баг, тут же исправлен (`f932596`).**
   Клик по строке «Точка Б» открывал калибровку «Точка А» — причина: обработчик для ЛЮБОЙ
   абсолютной точки вызывал общий `_calibrate()` → `calibration_ui.run_calibration()`, который
   всегда стартует с А независимо от того, что кликнули (`calibration_ui.py` не тронут, как
   требует Design 4.9). Исправлено — `_calibrate_single_point(target_id)` вызывает
   `calibrate_one_point()` напрямую с нужным `label`, результат сливается со ВТОРОЙ уже
   сохранённой точкой через новую чистую функцию `cal_merge_calibration_point` (2 теста —
   явно проверяют, что А и Б не путаются местами). Новый анти-паттерн записан в
   `ANTI-PATTERNS.md` — «комбинированный A+Б флоу на 2 разных входа».
   Заодно по запросу владельца: жёлтая рамка-подсветка выбранной строки (`#FFB300`, уже
   используемый в проекте акцент) вместо синеватой `MD3["elevated"]` — border, не заливка
   фона (не портит читаемость текста); нумерация строк «N. Название», сохраняется при смене
   языка. **Итог: 36/36 тестов калибровки зелёные, владелец подтвердил живьём — «проверил,
   сейчас всё хорошо».**
6. Память обновлена (`project_calibration_plan_a_coding_started.md` — полная история реализации
   и бага; `project_positionreader_user_calibration_scope.md`, `project_scout_priority_idea_parked.md`
   — идеи припаркованы отдельно), `MEMORY.md` индекс обновлён.

## ⚠️ Не сделано / решение владельца нужно

- **Версия бота (`version.py`) НЕ менялась (2.0.0), релиз НЕ собирался.** `main.py` изменён
  существенно (вкладка Калибровки переписана) — это клиентский код, попадающий в
  `TotalHunter.zip`. Нужно явное решение владельца: бампать версию и собирать релиз сейчас,
  или копить изменения дальше (например вместе с Part B/GUI Биржи 2.0) и выпустить одним
  релизом позже. Не решаю сам — сборка релиза только по команде владельца.
- Тест `test_rapid_sequence_of_row_clicks_final_state_wins` (файл 41, Test Matrix) НЕ
  реализован отдельным тестом — та же причина (виджет без harness'а), но логически избыточен:
  обработчик синхронный (Tk single-thread), доказательство уже дано в Stage 5 (файл 40).
- 10 предсуществующих падений тестов (`HuntEngine._bg_threads`) — обнаружены, НЕ трогались
  (вне scope этой сессии), стоит поднять отдельной задачей в будущем.

## Что делать в следующей сессии

1. **Дождаться дальнейшей обратной связи владельца** от продолжающейся живой проверки Калибровки
   (клик по каждой из оставшихся 10 строк, D-Pad на разных типах целей — OCR-область/точка/
   смещение-к-YOLO, сохранение/загрузка профиля, смена языка, видео-ссылка). Первый баг уже
   нашли и закрыли — вероятны ещё мелкие находки, память явно предупреждает не считать
   32-36 зелёных тестов доказательством отсутствия таких багов.
2. **Спросить про версию/релиз** (см. раздел выше) — если владелец решит бампать, следовать
   обычному регламенту релиза (`feedback_release_workflow`, чек-лист сборки из `CLAUDE.md`).
3. Когда Калибровка PLAN-A будет явно принята владельцем целиком — можно начинать **PLAN-B**
   (зелёная зона на 19 `CTkSlider`, C-07…C-10) — отдельный файл, следующий свободный номер 44.
   Не начинать раньше (решение Stage 0, файл 38).
4. Параллельно/после — рассмотреть **Part B Design** (конкурентность Биржи 2.0, файлы 29-31,
   Stage 1-3 уже пройдены, Design не начат) — туда же уходит идея пользовательской калибровки
   `PositionReader.crop_box` (`project_positionreader_user_calibration_scope.md`), не поднимать
   как самостоятельную задачу до старта Part B.
5. 10 предсуществующих падений тестов (`test_credit_guards.py`/`test_crypt_autostop.py`/
   `test_exchange_flow_v2.py`, `HuntEngine._bg_threads`) — не забыть, но и не начинать без
   отдельного запроса владельца (вне scope обеих текущих веток работы).

## Проверка сервера GCP (End-of-Session Check)

🟢 Все зоны зелёные: RAM 531Mi доступно из 969Mi, диск 59% (3.8G свободно), load average 0.12,
БД `totalhunter` 38MB, `totalhunter.service` active (перезапущен сегодня в 16:17 UTC для миграции
РОЙ), uptime сервера 134 дня. Очистка не требуется.
