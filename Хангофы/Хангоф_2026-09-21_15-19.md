# Хангоф 2026-09-21 15:19 (Kyiv) — Сессия #147, продолжение

Версия бота: `2.0.0` (не менялась, релиз не собирался — фича не готова к выпуску).
GCP сервер: 🟢 в норме (RAM 430MB/969MB, диск 59%, load 0.01/0.06/0.05, `totalhunter.service` active, БД 38MB).

## Что сделано

1. **Биржа 2.0 (Exchange Scout) впервые реально запущена через GUI:**
   - Кнопка Старт/Стоп на вкладке БИРЖИ (`self._scout_button`).
   - `active_mode` ('v1' / 'v2' / None) — взаимоисключение Биржи 1.0 и 2.0, включая ESC
     (`toggle_bot`, `_toggle_scout`, `_emergency_stop`).
   - Реальный захват экрана `mss.grab` + BGRA→BGR (`build_scout_capture_fn`) — тот же паттерн,
     что и в `PacmanEngine._run()`.
   - Реальный `crop_box` для чтения координат из калибровочной цели #13 `exchange_coord_roi`
     (`resolve_exchange_crop_box`) — то же вычисление, что и оверлей `_show_chest_rect_overlay`.
   - Переиспользована уже загруженная YOLO-модель `self.engine.model` (без повторной загрузки).
   - Цепочка `exchange_scout.py` (producer/consumer/OCR/spend_credit/RoyClient) НЕ изменена.
   - Коммит `3ebfbd5`.

2. **Живая проверка владельцем сразу нашла реальный баг:** глубина нырка Змейки в режиме Биржи 2.0
   бралась из ползунка Биржи 1.0 (`nav_inland_slider`, потолок 10 в GUI), хотя решение владельца по
   Бирже 2.0 — до 50 шагов вглубь (память `feedback_no_drama_on_bounded_number_changes`).
   - Причина, почему 38/38 тестов были зелёными при живом баге: тесты проверяли только
     `exchange_scout.py` напрямую с фейковым `navigator`, ни один тест не вызывал реальный
     production-путь `_toggle_scout`.
   - Исправлено: логика сборки `navigator` вынесена в `build_scout_navigator()` (module-level
     функция, как `resolve_exchange_crop_box`/`build_scout_capture_fn`) с константой
     `SCOUT_MAX_INLAND_STEPS = 50`. `_toggle_scout` вызывает ровно эту функцию.
   - Новый тест `test_max_inland_steps_is_50_not_the_bot1_slider_ceiling` дёргает ту же функцию,
     что и живой GUI — разрыв test/production wiring закрыт и задокументирован в `ANTI-PATTERNS.md`.
   - Коммит `d75b762`.

3. **Тесты:** 40/40 в `test_exchange_scout.py` зелёные. Широкая регрессия чистая — все падения
   (`test_credit_guards.py`, `test_crypt_autostop.py`, 4 ошибки коллекции при совместном запуске)
   подтверждены через `git stash` как предсуществующие, не связаны с этой сессией.

4. Оба коммита запушены в `origin/main`.

## Что делать в следующей сессии (по одному пункту за раз, без новых SPEC/DESIGN/PLAN-документов)

1. **Ползунки Биржи 1.0 на вкладке БИРЖИ должны переключаться на значения Биржи 2.0**, когда
   `active_mode == 'v2'` — сейчас глубина 50 (и другие параметры) это внутренняя константа/аргументы
   `build_scout_navigator()`, GUI её не показывает и владелец ей не управляет визуально. Владелец
   явно указал на это как на следующий шаг («переделывай»).
2. Конкретные числа для остальных ползунков/скорости Биржи 2.0 — по памяти
   `project_exchange20_concept_decisions.md`: «диапазон скорости другой (без нейросети быстрее),
   числа — при тестах» — НЕ придумывать самостоятельно, определить на следующей живой проверке.
3. Индикатор очереди/статуса (прогресс-бар, коричневый/зелёный из концепта Биржи 2.0, раздел
   «Статусы») — не реализован, владелец явно отметил «нет прогресс-бара».
4. Ещё не проверено живьём: змейка не ждёт YOLO (асинхронность продюсер/консьюмер), кадры реально
   копятся в `pending/`, YOLO реально работает параллельно, Биржа 1.0 не подменяет 2.0 в рантайме.
   Владелец явно попросил НЕ грузить это списком из 20 пунктов сразу — брать по одному, после
   очередной живой проверки в игре.

## Не забыть

- Никаких новых SPEC/DESIGN/PLAN документов по Бирже 2.0 — только код, тесты, коммит.
- Каждое исправление — сверять с реальным production-путём (`_toggle_scout` → фактический вызов),
  не только с изолированным модулем `exchange_scout.py` (см. новый анти-паттерн в `ANTI-PATTERNS.md`).
