# Хангоф 2026-09-26 01:10 — сессии #149 (25.09) и #150 (ночь 26.09)

Версия бота `2.0.0` (не менялась). Релиз не собирался. Сервер 🟢: RAM 459/969 МБ, диск 60%→59% после
`cleanup.sh`, load 0.04, БД 39 МБ, сервис active, прод = `6b5ef6f` (сегодняшние коммиты — только бот).

## Что сделано

**#149 (25.09, хангофа не было — фиксирую здесь):**
- Сундуки: конвейер (клик отдельно от OCR), лимит батча 5000, автоотправка по концу списка / лимиту
  (тумблер, по умолчанию выкл), прогресс-бар очереди OCR, динамическая высота строки, PNG-кропы,
  пол паузы клика 0.3 с, кнопка «Удалить батч», сохранённые пары Королевство+Клан.
- Окно бота ≤30% ширины экрана; взаимоисключение Склепы/Сундуки/Биржа 2.0; находки Биржи 2.0 в РОЙ.
- Сайт сундуков: ссылка `/c/{kingdom}/{slug}`, транслитерация клана, одна кнопка «Сохранить»,
  авто-объединение пресета, переводы 106/139.

**#150 (ночь 26.09):**
1. Ложное «Сервер недоступен (0/175)», очередь не чистилась. Причина: строгая сверка `count == len(items)`
   (`9e9a0a2`), а `count` — число НОВЫХ записей после дедупа. 175 → 172 на сервере (3 точных дубля),
   дальше повторы давали count=0. Фикс в `main.py`: при success батч помечается отправленным всегда.
2. По требованию владельца сделаны, затем по его же требованию ОТКАЧЕНЫ (`6eba94f`): удаление
   отправленных сундуков с ПК, единая база `%LOCALAPPDATA%\TotalHunter`, удаление модуля Combo,
   корневой `pytest.ini`. Код = `6b5ef6f` + правка отправки.
3. Вручную удалены отправленные строки из двух локальных баз (6396 + 106863), всё было на сервере.
4. Бот вставал после 2 сундуков: 3 проверки «кнопки нет» по 0.3 с (~0.6 с). По решению владельца
   `EMPTY_BUTTON_RETRY_PAUSE = 1.0` (`1164b6b`), скорость кликов не менялась (пол 0.3 с).
5. Живая проверка: 735 сундуков за 17 мин (00:45–01:02) без внештатной остановки, на сервере 701
   (ELDORADO 648 — клан владельца, Феникс 53 — мульт), 34 отброшены как дубли. Владелец: «всё верно».
6. Проверено: v2.0.0 у пользователей совместима с сервером (формат не менялся, успех = 2xx).
   По логам за 14 дней сундуки отправлял только владелец.

## Что делать в следующей сессии

1. **Дубли «игрок+тип+секунда»** (34 из 735, 3 из 175): выяснить, настоящие ли это разные сундуки
   (сервер их отбрасывает) или двойное чтение строки. Решение — за владельцем.
2. **Причина пропуска кнопки «Открыть» не доказана.** Если остановка повторится — снимать кадр в
   момент остановки (наблюдатель без кликов, только по команде владельца).
3. **Тесты бота:** полный `pytest` из корня собирает `server/tests` → `server/main.py` перекрывает
   `main.py` бота (4 файла падают на сборе); после исправления сбора видно ~90 старых падений
   (Combo, навигатор, ростер, турнир, credit_guards, crypt_autostop). Не чинились — откачено.
4. Хранение сундуков (одна база, удаление отправленного) — владелец откатил; возвращаться только
   по его решению. Учесть риск: при смене пути базы у клиентов теряется неотправленная очередь.
5. Релиз с этими правками — только по команде «собирай».
