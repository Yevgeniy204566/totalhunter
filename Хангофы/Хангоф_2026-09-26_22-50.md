# Хангоф 2026-09-26 22:50 — сессия #151

Версия: **2.1.0 (выпущена)**. Сервер GCP: 🟢 (RAM available 496Mi, диск 60%, БД 39 MB, сервис active).

## Что сделано

### Бот → релиз v2.1.0 (выпущен 26.09 ~22:07, GitHub v2.1.0 + сервер /version/latest = 2.1.0)
- Сундуки: каждому сундуку уникальное время (микросекунды) — сервер больше не схлопывает сундуки одного
  игрока/типа в одну секунду (538f204); одна база на ПК %LOCALAPPDATA%\TotalHunter + перенос старой;
  клан фиксируется в момент СТАРТ (cd07700); кропы очереди после аварийного перезапуска не теряются и не
  меняют клан (65d153c); отправленные удаляются с ПК сразу после успешной выгрузки (889478a).
- Живая проверка батчей 26.09 на сервере (read-only): 886 / 994 / 410 / 32 сундуков — 0 дублей, все
  времена уникальны, по 10 ◆ за отправку; новые ники сверены difflib — ни один не искажение старого.
- GUI: профиль + Сохранить/Загрузить — наверху Калибровки; профиль + 💾 наверху Склепов (✓/✗ 1 с);
  пары «королевство · клан» выше полей ввода; D-Pad-панель калибровки сразу под списком целей;
  списки профилей/кланов 16px жирные зелёные #22C55E (PICK_LIST_COLOR, pick_list_style()).
- Модуль «Комбинирование» удалён; тесты бота 56 падений → 0 (049874c).
- Перед сборкой: бот 766 passed / 0 failed / 6 skipped, сервер 511 passed / 0 failed. Nuitka 10 модулей,
  ZIP плоский 4087 файлов, sha256 0f164fae… совпал с залитым. Релиз создавался ЧЕРНОВИКОМ → владелец
  залил ZIP и опубликовал → только потом /admin/version/update.

### Сайт (всё выкачено на total-hunter.com)
- Лендинг: дракон, возможности списком с ✓, окно-предупреждение (Defender + калибровка) перед скачиванием;
  аудит устаревшей информации; пробные алмазы 300 везде (сервер TRIAL_CREDITS=300).
- Профиль: сохранённые таблицы кланов (users.saved_chest_links), «Собрано» склепы/биржи; Баланс —
  история пополнений; вкладки Охоты/Транзакции и верхние плитки убраны.
- Сундуки (сервер+кабинет): до 3 квот сезона вместо тумблеров (70b4e17, миграция q1u2o3t4a5s6), «Учёт»
  = Не в учёте / В учёте / Квота N; уровень Героя игрока (h1e2r3o4l5v6); квота EMC с личной целью по
  Герою (per_player, k и H₀ редактируемые, формула ВРЕМЕННАЯ); статистика сезонов CSV; спеки -01/-02.
- Публичная таблица: сортировка, строка средних (на телефоне скрыта), RU/EN, ссылка на главную,
  заголовок «ELDORADO 229», шапка закреплена (своя прокрутка высотой с экран — причина: overflow-x:auto
  у обёртки), колонка «Герой» первой слева (кнопка «Герой» прячет/показывает), ширина 1800px.
- Кабинет «Сундуки» — редизайн: квоты карточками в ряд (цвет = цвет квоты в «Учёте»), одна кнопка
  «💾 Сохранить сезон» (запуск = обновление = возобновление), вкладки 17px, полоса слева и линия между
  кланами, мини-таблица «Что означает Учёт», сворачиваемый ростер, читаемый список «Учёт».
- Гайд /guide#chests «Сундуки: учёт для клана» + свёрнутая копия в кабинете (один источник
  guide_content): запуск в боте, шаги, пресет (шаблон очков T5–T9), пример очков/квот таблицей, нюансы;
  шрифт гайда крупнее (основной 16–17px).
- Плагин frontend-design установлен (подхватится после перезапуска Claude Code).

## Что делать в следующей сессии

- Следить за автообновлением на 2.1.0 (скачивания ZIP на GitHub, жалобы в Telegram/Discord).
- Текст анонса 2.1.0 (RU/EN) выдан владельцу — публикует сам.
- Тип «Рейдовый рунический отряд ур. 40-44» в 18 из 60 случаев читается обрезанным «…отряд ур» — длинное
  название не влезает в зону «Источник (сундуки)». ЗАКРЫТО: владелец 26.09 — «забей», не чинить.
- Непривязанные к каталогу типы (Цитадель 30 уровня, Эпический отряд нежити, Богатство клана) — настройка
  в кабинете, не бот.
- Пауза сундуков 0.3 с — НЕ уменьшать вслепую; если владелец захочет — тестовый батч с 0.2 и сверка имён.
- Квота EMC: через ~неделю статистики владелец скажет про «походы/удары» (источник данных не выбран);
  через ~2 недели — замена временной формулы по CSV статистики.
