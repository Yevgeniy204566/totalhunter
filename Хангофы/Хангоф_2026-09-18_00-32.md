# Хангоф 2026-09-18 00:32 — сессия #142/143

**Версия на диске/в git:** `version.py` = `2.0.0`. Релизных изменений в этой сессии не было (только документация/спека) — новый релиз не требуется.

## Что сделано

1. Прочитаны входящие от Gemini (`docs/Входящие_Gemini.md`) — идея асинхронного сканера биржи (Producer/Consumer, фоновый YOLO).
2. Проведён брейнсторминг (superpowers:brainstorming, путь Architectural) нового режима **«Биржа 2.0» (Exchange Scout)** — recon-сканер, НЕ кликает по бирже, только находит и логирует координаты для последующей ручной обработки/донаведения. Биржа 1.0 не трогается ни строкой.
3. Аудит реального кода (лично прочитан, не по памяти): `navigator.py` (`CoastalSnakeNavigator`, `PositionReader`, `PacmanEngine`), `docs/exchange_bot_spec.md`, `calibration.py`, `debug_reporter.py`, фрагменты `main.py`. Подтверждено: `PositionReader` уже существует и простаивает (используется только в калибровке), глубина нырка (`max_inland_steps`) не клэмпится конструктором — 50 передаётся без правки `navigator.py`; `DIVE_DEPTH_CAP=10` ограничивает только авто-рост.
4. Дизайн архитектуры одобрен владельцем: буфер кадров на диск (не RAM), очередь без ограничения размера, отдельный файл `exchange_scout.py`, тумблер 1.0/2.0 внутри вкладки БИРЖИ, взаимное исключение запуска.
5. Владелец подтвердил: глубина до 50 — не отдельный риск (то же самое, уже протестированное поведение цикла, просто другое число итераций); «~200 скриншотов» — не константа, просто прикидка масштаба.
6. По протоколу `docs/РАБОТА-С-ДОКУМЕНТАМИ.md` (прочитан целиком) написана спека **Часть A** (файловый цикл, контракты переиспользования, GUI-интеграция) — через Opus-агента (обязательное правило модели для спек). Файл: `docs/superpowers/specs/2026-09-17-exchange-scout-part-a-lifecycle-design.md`.
7. Раунд 1 показан владельцу как черновик. Найдено и лично перепроверено кодом: X/Y из OCR — это координаты позиции экрана в момент кадра, НЕ координаты самой биржи (владелец в курсе, осознанно достаточно для визуального поиска на месте); формат каналов в `PositionReader` (BGRA vs BGR) не доказан эквивалентным — тест T-21; взаимное исключение 1.0/2.0 не покрывает мгновенный рестарт другого режима, пока старый ещё не завершился физически (передано в Часть B).
8. Владелец ответил на открытые вопросы: кредиты 2.0 списывает **по той же логике, что 1.0** (гейт баланса + `spend_credit` по факту находки); плюс новое требование — отправка находок в debug Telegram через уже существующий `debug_reporter.py` (плюс увеличенный кроп зоны OCR), пока владелец не уверен в надёжности детекции.
9. Раунд 2 внесён через Opus-агента: контракты C-17/C-18/C-19 (кредиты + debug Telegram), Adversarial Review A-14…A-18, тесты T-22…T-27, Self-Audit и Final Gate перепройдены.
10. Обновлён и сжат `MEMORY.md` (был близок к лимиту 200 строк) — часть старых записей объединена по темам, ничего не удалено физически.

## Что делать в следующей сессии

1. **🔴 Блокер по протоколу:** документ достиг раунда 2 (лимит) — по `docs/РАБОТА-С-ДОКУМЕНТАМИ.md` это ОБЯЗАТЕЛЬНАЯ остановка через `AskUserQuestion` (декомпозировать / продолжить ещё один раунд осознанно / решает владелец) ПЕРЕД любой следующей правкой. В этой сессии не было времени/токенов задать этот вопрос — задать его первым делом.
2. **Stage 8 (External Agent Review) теперь обязателен и не пройден.** Раунд 1 пропускал его законно (деньги были открытым вопросом, не контрактом). После раунда 2 монетизация — решённый контракт внутри Части A → условие «нет денег/concurrency в scope» больше не выполняется. Нужно продиспатчить узкого адверсариального ревьюера на весь документ (см. формулировку задания в протоколе, стадия 8), затем лично перепроверить находки по коду.
3. **Денежные вопросы, требующие явного «да» владельца** (нашёл Opus-агент в раунде 2, не решены):
   - 2.0 будет списывать кредит **чаще** 1.0 на доле ложных срабатываний YOLO — у 1.0 есть повторный `fresh_scan()` перед списанием, у 2.0 второго подтверждающего скана нет.
   - `spend_credit` в 2.0 вызывается из фонового consumer-потока, а не из GUI-потока (у 1.0 GUI на это время замораживается на ~5с) — осознанное расхождение, не подтверждено.
   - Серверный `/use_credit`/синхронизация баланса при потерянном сетевом ответе — не проверено лично (единственный неподтверждённый факт документа).
4. После явного одобрения владельца и прохождения Stage 8 — **Часть B** (конкурентность Producer/Consumer, очередь, graceful shutdown, race conditions между стартом/стопом 1.0 и 2.0). НЕ начинать её раньше закрытия Части A.
5. Полный текст решений и аудита — `MEMORY/project_exchange20_scout_brainstorm.md`.
6. **GCP End-of-Session Check (`docs/INFRA_HEALTH.md`) НЕ выполнен в этой сессии** — не было токенов на SSH-раунд. Выполнить в начале следующей сессии: `free -h`, `df -h /`, `uptime`, `systemctl status totalhunter`, размер БД.
7. **Не закоммичен** большой пласт старых untracked-файлов в корне репозитория (скриншоты вида «Админка.png», «Участники Клана.png», debug json/png/xls, build-логи) — репозиторий ПУБЛИЧНЫЙ, часть файлов похожа на личные/бизнес-скриншоты. Осознанно НЕ добавлены в этот коммит. Нужно отдельное решение владельца: удалить, добавить в `.gitignore` или разобрать вручную — не коммитить бездумно.
