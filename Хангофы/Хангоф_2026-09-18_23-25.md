# Хангоф 2026-09-18 23:25 — сессия #144

**Версия на диске/в git:** `version.py` = `2.0.0`. Код бота и сервера в этой сессии НЕ менялся (только документация и правила) — новый релиз не требуется.

**Проверка сервера GCP (End-of-Session, read-only):** 🟢 RAM 547Mi доступно из 969Mi, диск 60% (3.8G свободно), load 0.04, uptime 132 дня, `totalhunter` active, БД 38MB.

## Что сделано

### Правила работы (записаны в `CLAUDE.md`, `docs/РАБОТА-С-ДОКУМЕНТАМИ.md`, `ANTI-PATTERNS.md`, память)
1. **Агенты — только с явного «да» владельца** (предлагать можно, запускать нельзя); отменено правило «спеки только через Opus-агента»; Stage 8 — только по запросу владельца.
2. **Лимит раундов правок спек отменён** — правим, пока внешние ревьюеры (GPT, Gemini; ревью прогоняет владелец вручную) не одобрят.
3. **Никому не доверять слепо**: замечания ревьюеров, отчёты агентов, старые заметки — гипотезы; всё проверять по коду и документам; последнее слово за Claude.
4. **Работать только с тем, что владелец дал на ревью**; не анонсировать ревью, не аудитить чужие части заранее; вносить правки и коротко отчитываться.
5. **Биржа 1.0 неприкосновенна** — все решения по 2.0 живут отдельно.
6. Схема имён файлов спек/планов: цифры только одним числом в конце; у частей — буква после цифры (спека №1: 1a–1e; план: 12, 12a–12d).

### Спеки Биржи 2.0 (Exchange Scout)
- Прежняя спека Части A (1072 строки) разрезана и разделена на три спеки: **№1 публикация в «Рой»** (индекс + части 1a–1e), **№2 монетизация** (индекс + 4 файла, номера 4–7), **№3 Часть A: файловый цикл/GUI** (индекс + 4 файла, номера 8–11). Общий индекс `exchange-scout-index-16.md`. Все файлы ≤300 строк.
- **Спека №1 написана заново мной (без агентов)**: отдельный механизм публикации (`roy_scout_finds`, `POST /roy/scout-find`, публичный `GET /roy/scout-finds`, блок на `RoyPage.jsx`); срок показа 20 минут (собственная константа); не более 500 самых новых (`ORDER BY found_at DESC, id DESC`); `StrictInt` для kingdom/x/y с диапазоном 32-бит; PA-5 (запись без списания) — принятый остаточный риск; K фиксируется при создании результата; порядок 4.0: кадр → OCR → журнал (fsync) → списание → публикация; только aware UTC (PT-19); индекс по `found_at` (PT-20); PA-12 (`db.begin()` — замечание Gemini не подтверждено), PA-13 (конкуренция DELETE).
- **Спека №2 (монетизация)** доработана по ревью GPT: отдельный `hunt_type` (лимит 2 с снят только для 2.0, 1.0 не тронут), C-17(e)/(f) (после STOPPING нет новых списаний, неподтверждённая оплата не публикуется), C-21 (статистика 2.0 отдельной категорией), T-28…T-34, формулировка «результат списания неизвестен» вместо «неоплачена».
- **Внешнее ревью GPT+Gemini прошли (PASS):** главная часть спеки №1, часть 1a, часть 1b. Остальное не проходило: 1c, 1d, 1e; спеки №2 и №3 целиком.
- **План реализации публикации написан** (`plans/exchange-scout-roy-publication-plan-12.md`, `12a–12d`, задачи 1–6; деплой — гейт). Код серверной части плана прогнан во временной копии `server/` (репозиторий не менялся): **32 теста зелёные** (18 новых + 14 существующих РОЙ), мутации (убрать лимит/индекс/`id` в сортировке/`StrictInt`/aware-время/`>`) ловятся. Клиент (задача 4) и сайт (задача 5) не проверены.
- Убраны устаревшие упоминания Telegram в спеке №2; C-18/C-19 помечены «⛔ заменено».

## Что делать в следующей сессии
1. **Ревью частей 1c, 1d, 1e спеки №1** — по одной, когда владелец даст текст ревью (не аудитить заранее).
2. **Спеки №2 и №3** — внешнее ревью GPT/Gemini; в спеке №3 остаются упоминания debug-Telegram (ссылки на №2) — согласовать при её ревью.
3. **Планы:** `plans/…-12…` (публикация) не одобрены владельцем; плана для спеки №2 (серверный `hunt_type`, статистика, C-17(e)/(f)) нет; план consumer'а (Часть A/B) — после Части B. Кода нет и не начинать без одобрения.
4. **Часть B не написана**: ESC + non-daemon потоки, дренирование очереди после STOPPING, точка фиксации K, порядок «журнал → списание → публикация» в consumer'е.
5. **Концепт по входящим написан** (`docs/superpowers/specs/exchange-scout-concept-17.md` + части 17a–17e, черновик на ревью владельца): сверка спек с концептом; нужны решения владельца из 17e (точки калибровки 9 или 12, состав блока настроек, смысл скорости, 100% индикатора, кроп или весь кадр, определение «обход завершён»). Входящие Gemini (`docs/Входящие_Gemini.md`, изменён до сессии, не коммитился): разделы 3–6 и 8 (сворачиваемый блок настроек змейки, скорость, очередь и прогресс-бар, пауза 300/30, цвета кнопки, индикатор статуса) не сверены со спеками; калибровка на 9 точек и рекомендуемые значения ползунков — отдельная задача вне Биржи 2.0.
6. Решение владельца при желании: перевести спеки №2 и №3 на схему имён «цифра + буква» (сейчас у их частей сквозные номера 4–11).
7. Большой пласт старых untracked-файлов в корне (скриншоты, debug json/png/xls, build-логи) по-прежнему НЕ коммитится — публичный репозиторий; нужно решение владельца (удалить / `.gitignore` / разобрать).

## Продолжение сессии (после основного хангофа, до 2026-09-19)

### Что сделано
- **Концепт по документу «Входящие»** (разделы 1–9): `docs/superpowers/specs/exchange-scout-concept-17.md` + части 17a–17e (калибровка и ползунки; настройки змейки и скорость; очередь и балансировка; найденный кадр и статусы; совместимость, сводка, конфликты). Факты кода проверены (`main.py`, `navigator.py`, `debug_reporter.py`, `server/debug_router.py`), числа в концепт не вписывались.
- **Решения владельца** (записаны в концепт и в память `project_exchange20_concept_decisions.md`): 12 точек калибровки; кадр удаляют только consumer после обработки или бот через 30 минут; `ESC` останавливает змейку, но не нейросеть (для неё отдельная кнопка); пауза = автоматический Стоп/Старт; 100% индикатора = порог паузы (~300), возобновление при 10%; статусы коричневый/зелёный; индикатор в стиле бота; блок настроек змейки как у обычной; видео — ссылка YouTube; сайт 20 минут от публикации оставлен; временный админский debug-шаг (кроп зоны OCR в debug Telegram).
- **Правки спек по концепту:** спека №1 (шапка: Telegram не как канал публикации; Stage 1 п. 5; 4.0 — удаление кадра после передачи результата), спека №2 (баннеры, A-15, видимость находок — по журналу), спека №3 (баннер пересмотра; C-02, C-03, C-08, T-01/02/03/08, A-6, non-goal 6, абзац о повторном запуске помечены «⚠ ПЕРЕСМОТРЕН» с новым инвариантом конвейера; полная переработка — при ревью спеки №3 и в Части B).

### Что делать в следующей сессии (дополнение)
1. Ревью 1c, 1d, 1e спеки №1; спеки №2 и №3 (в №3 — полная переработка C-02/C-03/C-08 и структуры `pending/found` под инвариант конвейера).
2. **Часть B:** устройство очереди и папок при перезапуске (по `sid` или единая очередь), остановка нейросети кнопкой и `ESC`, удаление устаревших (30 мин), состояния коричневый/зелёный, non-daemon поток публикации.
3. Спека GUI Биржи 2.0 (трек B концепта) и отдельная спека калибровки/ползунков (трек C) — по команде владельца.
4. Контракт временного админского debug-шага (админ-гейт: у `/api/debug/*` нет аутентификации; содержимое; выключаемость).
