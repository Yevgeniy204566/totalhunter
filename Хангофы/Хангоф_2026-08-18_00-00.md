# Хангоф 2026-08-18 00:00 — Сессия #129

## Что сделано

1. **Закрыта брешь бесплатного фарма Склепов + offline-guard, релиз v1.8.15**
   - Диагностика (независимая, не слепо по Gemini): `on_crypt_found` (main.py) игнорировал `low_credits`/сетевые ошибки `spend_credit()` — не останавливал бота при 0 балансе, в отличие от биржевой ветки.
   - Второй сценарий: точечная блокировка процесса `TotalHunter.exe` файрволом (игра+интернет работают, бот — нет).
   - Фикс (TDD, `test_credit_guards.py`+`test_auth_contact_guard.py`, 19 новых тестов зелёных, 41 регресс без изменений):
     - `auth.py`: `HEARTBEAT_TIMEOUT=120` (согласовано с владельцем), `seconds_since_last_contact()`.
     - `main.py`: `on_crypt_found` + `_process_found` — оба останавливают бота на `low_credits` и на offline-guard.
     - Текст «Остановлено: нет связи с сервером» на всех 19 языках.
   - Релиз v1.8.15: Nuitka 10/10 модулей, ZIP 447025529 байт проверен (`curl` 200 OK), [GitHub Release](https://github.com/Yevgeniy204566/totalhunter/releases/tag/v1.8.15), `/admin/version/update` → `/version/latest` подтверждён. GCP не трогали.

2. **Перенесена методология автономности из Nutrition** (по просьбе владельца, с явным уточнением через вопрос — барьер «да перед кодом» смягчён для рутины)
   - `CLAUDE.md` раздел 4 — обновлён WORKFLOW (рутина автономно, архитектура/числа/дизайн/сборка — жёсткий gate) + добавлено правило «один поток по умолчанию» (не переключаться на новую задачу молча).
   - `CLAUDE.md` раздел 7 — добавлен шаг 7 хангофа (файл в `Хангофы/`) + пункт про папку.
   - Новая память: `feedback_autonomous_work_protocol.md` (полный протокол, адаптирован из Nutrition).
   - Обновлена `feedback_no_initiative.md` — сужена до решений (числа/дизайн/архитектура), не рутины.
   - Создана папка `Хангофы/` — этот файл первый в ней.

## Что делать в следующей сессии

- Мобильный лендинг (**в работе прямо сейчас, не завершено к моменту этого хангофа**): заменить первый скриншот на короткое видео, уменьшить скриншоты, разместить их под видео (Склепы → Биржи), под ними — блок статистики «найдено бирж / собрано Склепов / всего игроков» друг за другом (вертикально).
- ⏸ Kill-Switch для старых версий бота — всё ещё ждёт «да» владельца (см. `project_version_kill_switch_proposal.md`).
