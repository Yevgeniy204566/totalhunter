# Хангоф 2026-09-10 21:55 — Сессия #139

## Что сделано

Версия на момент сессии: **1.9.2** (без изменений — код-фиксов в самой охоте не делали).

1. **Диагностика воронки регистраций** — владелец спросил, почему регистрации идут, а 300
   подарочных алмазов почти никто не тратит. Разобрано через `/admin/stats`, `/admin/users`,
   `/admin/activity/{crypt,exchange}`, `/admin/crashes`, `/admin/logs` (curl с публичным
   `ADMIN_TOKEN` — не прямой доступ к БД). Итог: ~45% всех User-записей — Google-логин на сайте
   без HWID, к боту не относятся. Из 18 реальных запусков бота за 3 недели — 8 получили 300+
   алмазов и сделали 0 попыток охоты, 4 не дошли даже до `/claim_trial`, только 2 сделали ровно
   1 попытку и остановились. Первая гипотеза (баг зависания `_scroll_and_find`, сессия #138) была
   обоснованно оспорена владельцем — у него самого (3 игрока, 20к+ склепов) и у платящих клиентов
   охота идёт стабильно, массовое зависание при первом запуске нереалистично. Пересмотрено: найден
   ПОДТВЕРЖДЁННЫЙ баг — `0f77c02` (вошёл только в 1.9.2) чинил двойной трансформ координат клика
   «Капитан» в браузерных профилях (Chrome `scale_y≈0.885` — молчаливый промах, без ошибки). 7 из 8
   «не потратили» сидели на версиях до этого фикса. Полная запись → `MEMORY/project_onboarding_funnel_diagnosis_sep10.md`.

2. **Найден и закрыт разрыв git↔релиз.** Релизы v1.9.0/v1.9.1 (GitHub-теги существуют) реально
   вышли пользователям, но их код (`calibration_ui.py`, `profiles/*.json`, `gui_config.json`,
   тесты) никогда не коммитился — сборка идёт с диска (Nuitka пакует рабочую папку), а не из git
   checkout, поэтому код физически ушёл в exe, но не был закоммичен/ревьюим/восстановим из истории.
   По прямому запросу владельца («закомить всё что надо, на автопилоте, без вопросов») —
   закоммичено: `14ab7bf` (код: масштаб прицела калибровки под 2K/4K, актуальные
   `profile_chrome.json`/`profile_client.json`, `gui_config.json`, тесты, `.gitignore`), `3646bbe`
   (буфер Gemini), `355b408` (документация). Правило закреплено постоянно (не разовое разрешение)
   в `CLAUDE.md` (пункт 0 чеклиста перед сборкой) и `ANTI-PATTERNS.md` + память
   `feedback_always_commit_release_code.md`.

3. Не тронуты (по решению — отдельная задача, не сегодня): куча untracked мусора в рабочей папке
   (скриншоты, `_dbg_*.json`, build-логи, `MEMORY_BACKUP/`, `.codex/`, `AGENTS.md`).

## Что делать в следующей сессии

**Баги из сессии #138 (диагностика зависания CryptHunter) — фикс НЕ согласован, решение за
владельцем:**
- (а) верхний предел попыток + вернуть закомментированный построчный лог в `_scroll_and_find()`
  (`crypt_hunter.py`) — сейчас при зависании не остаётся вообще никакого следа.
- (б) детект диалога «недостаточно масла» после `_send_captain()` («нажать Исследовать») —
  **важное уточнение владельца от 2026-09-10:** OCR-распознавание масла (`_ocr_region()`)
  СОЗНАТЕЛЬНО отключено (раньше давало ложные срабатывания, срывавшие цикл) — то есть у бота
  СЕЙЧАС физически нет ни одного способа понять, что масла не хватило. Он просто крутит цикл
  кликов дальше, как будто всё в порядке («бот просто крутит колесо кликов и всё» — дословно
  владелец). Если делать детект — то ТОЛЬКО template-matching по картинке диалога, не OCR (OCR
  не предлагать без явного отдельного запроса владельца).
- (в) общий watchdog «нет прогресса N минут → мягкая остановка с понятным сообщением, не
  молчаливое зависание».
- Владелец сам не помнит точный список фиксов на момент этой сессии — при следующем разговоре
  свериться с этим списком и с `MEMORY/project_crypt_hang_investigation.md` заново, не считать
  вопрос закрытым и не выбирать конкретный фикс без нового «да».

**Воронка регистраций:**
- Баг координат в Chrome (`0f77c02`) уже исправлен в 1.9.2 — наблюдать, улучшится ли конверсия
  новых Chrome-игроков в ближайшие недели (сравнить долю «0 попыток» среди новых 1.9.2-регистраций
  vs старых версий).
- Группа «0 попыток вообще» (не про масло, они не доходят даже до старта) — причина не
  подтверждена логами (телеметрия не фиксирует «открыл и закрыл от растерянности»). Если будет
  время/интерес — можно списаться с 3 юзерами из этой группы, у кого есть привязанная почта
  (`talita852789@gmail.com`, `mad-mak@yandex.ru`, `arsaltamd@gmail.com`), спросить что пошло не
  так на первом запуске.

**Технический долг (не срочно):**
- Untracked мусор в `C:\BattleBot` — разобрать отдельной сессией: что нужно закоммитить, что в
  `.gitignore`, что удалить.
