# Калибровка — Implementation Plan A (единый реестр 12 целей), часть 41: Стадия 6 (Test Matrix)

> Спека-источник: [`exchange-scout-calibration-design-35.md`](../specs/exchange-scout-calibration-design-35.md)
> (+ [context-32](../specs/exchange-scout-calibration-context-32.md) ·
> [archaeology-33](../specs/exchange-scout-calibration-archaeology-33.md) ·
> [contracts-34](../specs/exchange-scout-calibration-contracts-34.md) ·
> [adversarial-36](../specs/exchange-scout-calibration-adversarial-36.md) ·
> [tests-gate-37](../specs/exchange-scout-calibration-tests-gate-37.md), Final Gate пройден 2026-09-19,
> коммит `06c2e13`). Протокол: `docs/РАБОТА-С-ДОКУМЕНТАМИ.md` (10 стадий).
> Части плана: [38](exchange-scout-calibration-plan-a-context-38.md) (Стадия 0+1) ·
> [39](exchange-scout-calibration-plan-a-contracts-39.md) (Стадия 2+3, PC-01…PC-07) ·
> [40](exchange-scout-calibration-plan-a-design-40.md) (Стадия 4+5) ·
> [41](exchange-scout-calibration-plan-a-tests-41.md) (этот файл, Стадия 6).
> Раунд правок: 1.

---

## Стадия 6 — Test Matrix

Формат (протокол, раздел 6): `Invariant | Normal | Boundary | Failure | Concurrent | Recovery | Test`.
Concurrent в этом PLAN почти всегда N/A по одной и той же структурной причине — Tk-приложение
однопоточное, GUI-состояние (`self._cal_selected_id`, видимость `tune_card`) не имеет фонового
воркера/сетевого вызова, конкурентно его меняющего (подтверждено Stage 5, файл 40: `run_calibration`
модален, окно свёрнуто на время absolute-сеанса). Recovery в большинстве строк также N/A — реестр
статические данные кода, а не персистентное состояние с восстановлением после сбоя/рестарта; сохранение
на диск (`_save_profile`) не входит в PLAN-A и не меняется.

| Invariant | Normal | Boundary | Failure | Concurrent | Recovery | Test |
|---|---|---|---|---|---|---|
| C-01: единственная точка входа для выбора цели (Design 4.2/4.3, файл 40 — уточнение Self-Audit, файл 42: PC-03 доказывает identity, не «один вход», отдельный тест был нужен) | После реализации `hasattr(self, "_cal_manual_btn")` и `hasattr(self, "_tune_option_menu")` — оба `False`; единственный путь выбора — клик по строке `_target_list_frame` | N/A | Любой из двух старых виджетов пережил рефакторинг как рабочий (не просто мёртвый неиспользуемый атрибут, а всё ещё что-то делающий) — второй независимый вход, нарушение C-01 | N/A | N/A | `test_calibration_has_single_target_selection_entry_point` |
| PC-01: реестр — ровно 12 записей, обязательные/запрещённые поля по kind/storage | 12 записей, состав совпадает с Design 4.1 построчно | `chest_collect` — граница по ПОЛЯМ, не по теме: `kind="точка"`, `storage="offset"`, `base_pos_fn` присутствует и `callable`, `ref_rect` отсутствует (тематическая близость к «сундукам» на классификацию не влияет — сверяется whitelist PC-01, файл 39, не «на глаз») | `ref_rect` отсутствует у `chest_sender`/`chest_type`; `base_pos_fn` отсутствует у одной из 7 offset-точек (полный список — PC-01, файл 39); лишнее поле там, где запрещено | N/A (статические данные) | N/A | `test_registry_has_exactly_12_entries_matching_design_table`, `test_registry_optional_fields_present_only_for_matching_kind` |
| PC-01 (доп.): `label_key` существует во всех 19 `LANGS` | Все 12 `label_key` найдены в каждом из 19 языковых блоков | Ключ есть в 18 языках, отсутствует в одном (реалистичный дрейф — добавили цель, забыли один перевод) | `KeyError` при выборе цели на языке с отсутствующим ключом | N/A | N/A | `test_registry_has_exactly_12_entries_matching_design_table` (расширенная проверка, Стадия 5 файла 40, находка 6) |
| PC-02: `registry[storage=offset].id` == `_UI_BUTTON_NAMES` (двустороннее) | Множества идентичны, 10 элементов с каждой стороны | Один и тот же элемент представлен разным регистром/опечаткой (латиница/кириллица визуально похожи) — сравнение строгое `==`, не case-insensitive | Имя в реестре без пары в `_UI_BUTTON_NAMES` (офсет недоступен для правки); имя в `_UI_BUTTON_NAMES` без пары в реестре (`set_ui_offset()` молча no-op, `coord_manager.py:87-90`) | N/A | N/A | `test_registry_offset_ids_match_ui_button_names_bidirectionally` |
| PC-03: identity выбора по `id`, не по индексу/переводу | Выбрана `wt_icon` → смена языка (RU→EN) → выбор остаётся `wt_icon` | Выбор `None` (ничего не выбрано) при смене языка — обновляются только подписи списка, панель не появляется | N/A (структурно исключено — `id` не пересчитывается из индекса, сравнение по строке) | N/A | N/A | `test_change_lang_preserves_selection_by_id_not_index` |
| PC-04: `TUNE_TARGET_NAMES`/`_TUNE_POINT_TARGETS`/`_CHEST_TUNE_RECTS` полностью удалены | `grep` по всему проекту после реализации — 0 совпадений | N/A (бинарное условие — есть/нет) | Забытая ссылка на одну из трёх структур в недописанном месте (тот же класс, что AP-DUP-HARDCODE) | N/A | N/A | `test_no_legacy_tune_structures_remain` |
| PC-05: `base_pos_fn` вызывается заново на КАЖДЫЙ рендер/выбор (не только «после recalibrate»), не кэшируется | Выбрана `wt_icon` → крестик в правильной точке; выбор той же `wt_icon` повторно (или повторный рендер без пере-калибровки) → `base_pos_fn` вызвана СНОВА (счётчик вызовов растёт), не переиспользован сохранённый результат первого вызова | Пере-калибровка `ref_a`/`ref_b` (новые точки A/Б) во время открытой панели `wt_icon` → крестик СРАЗУ отражает новую калибровку при следующем показе, без перезапуска бота | Два разных сценария регрессии, оба обязаны ловиться: (а) `base_pos_fn` вызвана один раз и результат сохранён как число — рендер #2 без пере-калибровки НЕ вызывает функцию снова (счётчик не растёт, старый тест этого не поймал бы — реальная находка ревью); (б) кэш инвалидируется только по recalibrate, а не отсутствует вовсе — совпадёт с (а) при обычном повторном рендере | N/A | N/A | `test_base_pos_fn_called_on_each_render_and_reflects_recalibration` — «рендер» операционально определён: каждый вызов `_tune_refresh_display()` (`main.py:5144`, единственный вызывающий `_tune_show_point_crosshair_if_relevant()` → `base_pos_fn()`, проверено по коду) при выбранной offset-точке. spy/счётчик на `base_pos_fn` (mock, считает вызовы): вызов `_tune_refresh_display()` #1 → count=1; вызов `_tune_refresh_display()` #2 той же цели БЕЗ recalibrate → count=2 (доказывает «каждый рендер», не только «после recalibrate»); recalibrate → вызов `_tune_refresh_display()` #3 → count=3 И новая координата (заменяет `test_base_pos_fn_reflects_new_calibration_after_recalibrate` — та же идея, усилена счётчиком) |
| PC-06: единый визуальный путь на `kind` — все 3 ветки, не только regression-детали | `точка` → `_show_crosshair_at`; `OCR-область` → `_show_chest_rect_overlay`; `смещение-к-YOLO` → текст «недоступно» — все три диспетчеризуются верно, не только формулировкой Normal-колонки | `ref_a`/`ref_b` (точка/absolute) используют тот же `_show_crosshair_at`, что и точки/offset — не `_show_red_dot` | Любая из веток вызывает не тот визуальный путь; `_show_red_dot` вызван из нового списка для `ref_a`/`ref_b` (регрессия на старый путь, C-03 нарушен) | N/A | N/A | `test_one_rendering_path_per_kind` (spec T-03, `tests-gate-37.md` — уже покрывает broad-диспетчеризацию ПО ВСЕМ трём `kind` И явное неиспользование `_show_red_dot` в новом UI; PLAN-A реализует этот уже названный spec-тест, не изобретает параллельный) + `test_ref_a_ref_b_use_crosshair_not_red_dot_in_new_list` (узкий PLAN-специфичный regression-тест конкретно на миграцию `ref_a`/`ref_b`, дополняет T-03) |
| PC-07: видео-ссылка — заглушка, не битая ссылка | `CALIBRATION_VIDEO_URL` непустой → клик вызывает `webbrowser.open(url)` | Ровно пустая строка `""` — элемент неактивен | Обработка отказа ОС/браузера при `webbrowser.open()` — вне scope PLAN-A (контракт C-06 требует только active/inactive dispatch по наличию URL, не устойчивость к сбою внешнего процесса; поведение `webbrowser.open()` при недоступном браузере не утверждается и не проверяется этим PLAN) | N/A | N/A | `test_video_link_inactive_when_url_empty`, `test_video_link_calls_webbrowser_open_when_url_set` |
| Видимость `tune_card` — 3 состояния (Design 4.4, файл 40) | `selected_id=None` → скрыт; `storage=offset` → показан | Переход `offset → absolute`: `wt_icon` выбран (панель видна) → клик `ref_a` → панель скрыта ДО вызова `_calibrate()` и остаётся скрытой ПОСЛЕ (оба исхода: подтверждено/отменено) | Панель осталась видимой для `ref_a`/`ref_b` — пользователь видит D-Pad от `wt_icon`, потенциально кликабельный для цели без `ui_offsets`-записи (PC-02) | N/A (Tk single-thread, `_calibrate()` модален — переход не может прерваться на середине кликом) | N/A | `test_selecting_absolute_target_hides_offset_editor` |
| Быстрая последовательность кликов по РАЗНЫМ строкам (уточнение ревью, не было отдельным тестом до этого раунда) | Один клик → одно применение состояния | Быстрая последовательность `offset A → offset B → offset C → ref_a` за короткий промежуток (не гонка потоков — Tk обрабатывает события строго по очереди, но нужно доказать, что финальное состояние определяется ПОСЛЕДНИМ событием, а не смешивается с промежуточными) | Финальное состояние не совпадает с последним кликом (например панель `offset C` осталась видна после `ref_a`) — означало бы, что 4.4 применяется не на каждое событие, а с накоплением/гонкой | N/A (структурно невозможно при однопоточном `bind`, но контрпример стоит доказать тестом, не только рассуждением — правило протокола Stage 5 «после каждого найденного защитного механизма — контрпример») | N/A | `test_rapid_sequence_of_row_clicks_final_state_wins` — эмитировать 4 события `<Button-1>` подряд без ожидания между ними (`event_generate` синхронно в тесте), проверить `self._cal_selected_id == "ref_a"` и `tune_card` скрыт |
| Явный `0` доходит до `coord_manager` как есть (анти-паттерн Стадии 1, файл 38 — находка Self-Audit, файл 42) | D-Pad уменьшает офсет с `(1, 0)` до `(0, 0)` → **сам вызов** `coord_manager.set_ui_offset(id, 0, 0)` реально произошёл (проверяется через mock/spy на `set_ui_offset`, не через последующее чтение `get_ui_offset()` — иначе тест проходит и в контрпримере "код молча пропускает запись, когда итог и так 0", т.к. предыдущее значение уже могло быть 0) | Клик D-Pad ПОСЛЕ достижения `(0,0)` в отрицательную сторону → `set_ui_offset(id, -1, 0)` вызван (переход через ноль не блокируется и не даёт спецповедения) | Реализация нового `_tune_get_key()`-эквивалента (единственная точка, которую меняет PLAN-A, `main.py:5107` сегодня) добавляет `if dx or dy:`-подобную проверку перед `set_ui_offset`, из-за которой при обнулении вызов `set_ui_offset(id, 0, 0)` не происходит — mock фиксирует отсутствие вызова, тест ловит несовпадение (найдено ревью: тест через `get_ui_offset()` эту регрессию НЕ ловит, т.к. пропущенная запись в 0 внешне неотличима от успешной) | N/A | N/A | `test_dpad_zero_offset_is_written_to_coord_manager` — `assert_called_with(id, 0, 0)` на шаге к нулю, `assert_called_with(id, -1, 0)` на следующем шаге в минус |
| Нет autosave на каждый D-Pad клик (анти-паттерн Стадии 1, файл 38 — находка Self-Audit, файл 42) | Клик D-Pad → `coord_manager.ui_offsets` меняется в памяти; факт отсутствия записи на диск наблюдается напрямую — либо `mtime`+содержимое файла профиля до/после клика не изменились, либо (если `_save_profile()` — единственная и стабильно spy-able функция записи профиля, `main.py:4988`) `assert_not_called()` на неё — выбор конкретного способа фиксируется при реализации теста, не оставляется общей фразой «не записывает» | N/A | `_save_profile()`/фактическая запись файла происходит из обработчика D-Pad-клика — новый источник ключа (`self._cal_selected_id`) случайно потянул за собой старый побочный эффект при рефакторинге; тест ловит это по неизменности файла/`mtime`, не только по отсутствию вызова конкретного имени функции | N/A | N/A | `test_dpad_click_does_not_write_profile_to_disk` |

## Что дальше

Следующий шаг — Стадия 7 (Self-Audit): отдельный проход (не продолжение написания) — грепнуть текст
файлов 38-41 на `MUST/ОБЯЗАН/РОВНО/ВСЕГДА/НИКОГДА/максимум/минимум/unique/exactly/at
most/at least/atomic/idempotent`, для каждого найденного места подтвердить конкретный enforcement-
механизм; отдельно — requirements coverage (каждый C-01…C-06 спеки покрыт PC-01…PC-07 плана), scope
creep (не протащено ли что-то из PositionReader/OCR-geometry/PLAN-B за 6 раундов правок).
