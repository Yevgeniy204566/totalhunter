# Биржа 2.0 (Exchange Scout) — ПУБЛИКАЦИЯ НАХОДОК НА САЙТ В РАЗДЕЛЕ «РОЙ»

> **Статус:** ЧЕРНОВИК. **Спека №1 из 3** — первая в очереди на внешнее ревью (GPT, Gemini): не пройдено. Решения владельца учтены (срок жизни находки 20 мин, ОВ-1 закрыт). К реализации НЕ готова. Общий индекс: [`exchange-scout-index-16.md`](exchange-scout-index-16.md).
> **Тип:** SPEC (не PLAN, не код). **Дата:** 2026-09-18. **Раунд правок: 7** (раунд 2 — закрытие ОВ-1; 3–7 — замечания внешнего ревью и полный самоаудит, каждое проверено по коду; лимит раундов отменён 2026-09-18, правим до одобрения GPT/Gemini)
> **Протокол:** `docs/РАБОТА-С-ДОКУМЕНТАМИ.md` (прочитан целиком в этой сессии). Стадии 1-7, 9 пройдены.
> **Stage 8:** не запрошен владельцем (золотое правило 2026-09-18: агенты только с явного «да»). Вместо него
> сделан второй самостоятельный адверсариальный проход по коду (стадия 5, блок PA-*).
> **Автор:** Claude лично, в основном диалоге, без субагентов.
> **Соседние спеки:** Часть A (файловый цикл, GUI) и монетизация (списание 10◆) —
> `exchange-scout-part-a-lifecycle-design-3.md`, `exchange-scout-monetization-design-2.md`.
> Этот документ заменяет в них канал debug-Telegram (C-18/C-19): **Telegram для 2.0 не используется.**

**Scope:** ровно один шаг цепочки 2.0 — «обработанный скрин дал координаты → находка выложена на сайт в
разделе РОЙ». Списание, файловый цикл, очередь, GUI — вне документа.

---

## Стадия 1. Context

Цепочка 2.0 (решение владельца, дословно по смыслу): **YOLO нашёл биржу → списать 10◆ → показать пользователю
находку → обработать сохранённый скрин → получить из него координаты → выложить находку на сайте в разделе
«Рой».**

Закрытые решения владельца, не предмет обсуждения здесь:
1. Telegram для публикации не используется — только сайт.
2. Механизма выкладки на сайт нет — его проектирует этот документ.
3. Дедупликация находок сейчас НЕ нужна (никакой защиты от повторной находки одной биржи).
4. Биржа 1.0 и её код/защиты не меняются (`feedback_exchange_10_untouchable`).
5. Очередь скриншотов не ограничена, кадры не выбрасываются (пауза змейки на пороге — балансировка, не лимит).
6. X/Y из OCR — координаты позиции экрана в момент кадра, а не самой биржи; владелец знает, что этого
   достаточно для визуального поиска на месте («примерное расположение»).
7. Серверная атомарная связь публикации с успешным списанием в этой версии **отсутствует и не реализуется**: P-01 — контракт штатного клиента, а не серверный security-инвариант (PA-5). Новые механизмы защиты (payment-token, rate limit, дедупликация, retry) без решения владельца не вводятся.

Что считается «сайтом»: `https://total-hunter.com`, страница РОЙ (`web/src/pages/RoyPage.jsx`), API —
`https://api.total-hunter.com` (`roy/roy_client.py:8`).

## Стадия 2. Code Archaeology — что реально есть (всё открыто и прочитано лично)

**2.1 Страница РОЙ на сайте показывает только королевства, координат на ней нет.**
`web/src/pages/RoyPage.jsx`: данные — `fetch(.../roy/kingdoms)` (:25) и `EventSource(.../roy/status-stream)`
(:30); тело страницы — список `kingdoms.map(...)` (:84-110): точка статуса, номер королевства, счётчики.
Ни `pool`, ни `x/y` в файле нет (grep по `pool|coord|\.x\b` нашёл лишь текст meta-описания :19).
Текст meta говорит «Коллективный пул координат бирж» — на странице этого пула нет. Страница двуязычная
(`isRu`), других языков нет.

**2.2 `POST /roy/report` — существующий канал координат 1.0 (`server/roy.py:244-294`).**
- Схема `ReportRequest`: `hwid, kingdom, x, y, percent` — `percent: int` **обязателен** (:192-197). У 2.0
  процента нет: диалог биржи не открывается.
- Rate limit `RATE_LIMIT_SEC = 10` с одного hwid (:39, :256-258): при превышении возвращает
  `{"success": True, "note": "rate_limited"}` — **молча отбрасывает** запись, клиент видит «успех».
- Дедуп по (kingdom, x, y) с продлением TTL (:266-278); `UniqueConstraint` в `models.py:274-277`.
- TTL записи 20 мин (`POOL_TTL_MIN = 20`, :33).
- После записи — `_broadcast(db, pool_updated=True)` (:289) → SSE-подписчики.
- При новой записи во время ивента «Торговые Пути» — тизер в Telegram (:291-292, `send_telegram_alert`).

**2.3 Пул `roy_pool` потребляют боты 1.0.** `GET /roy/pool` стоит `POOL_COST_SEC = 60` баланса времени (:37);
клиент — `roy/roy_client.py:87-96`; в `main.py:4213` новые записи пула вызывают звук (`truly_new`).
→ Запись примерных координат 2.0 в `roy_pool` подсунула бы ботам 1.0 неточные данные и изменила бы поведение
1.0. **Переиспользовать `/roy/report` и `roy_pool` для 2.0 нельзя** (вывод из 2.1-2.3).

**2.4 Источник номера королевства.** `PositionReader` K не читает; в `scout_results.jsonl` поля `kingdom` нет
(Часть A, C-10, `exchange-scout-part-a-contracts-design-9.md` §4.6). В GUI есть поле номера королевства:
`self._roy_kingdom_entry` (`main.py:3955-3970`), `_get_roy_kingdom()` возвращает `0`, если поле пустое или не
число (`main.py:4141-4145`). Сервер показывает только королевства с `k > 0` (`roy.py:130`).
Решение владельца: номер королевства берётся от участников, исследующих это королевство, — они вносят его
**сами и добровольно** в это поле; бот его не определяет и не проверяет. Публикация 2.0 использует то же поле, но значение фиксируется при создании результата находки (P-06).
**Номер королевства (со слов участника) и X/Y (прочитаны OCR с сохранённого скрина, где YOLO нашёл биржу)
независимы друг от друга** — из одного источника они не выводятся и друг с другом не сверяются.

**2.5 Клиент РОЙ.** `RoyClient.report()` — fire-and-forget в отдельном **не-daemon** треде, `_TIMEOUT = 5`,
все исключения проглатываются (`roy/roy_client.py:9, 27-48`). Это готовый прецедент формы клиентского вызова.

**2.6 Валидация hwid на сервере — прецедент.** `/use_credit` проверяет существование пользователя и бан:
404 «User not found» / 403 «Banned» (`server/main.py:324-332`). `/roy/report` hwid не проверяет вообще.

**2.6a Длина hwid.** `User.hwid` — `String(16)` (`server/models.py:65`), `get_hwid()` возвращает ровно 16 символов (`auth.py:35`), `reporter_hwid` — `String(16)` как у `RoyPool.reporter_hwid` (`models.py:284`). Вставка возможна только для hwid, найденного в `users` (P-05), поэтому длина ≤16 гарантирована самим существованием пользователя; неизвестный или слишком длинный hwid → 404 до вставки.

**2.7 Тесты сервера РОЙ.** `server/tests/test_roy.py`: `pytest.mark.asyncio`, `AsyncClient` +
`ASGITransport`, фикстура `db_session`, autouse `clear_roy_rate_limits` (:17-18).

**2.8 Аналоги.** Публичного списка координат в РОЙ **нет** (2.1). Ближайший фундаментальный механизм —
пара «клиентский метод `RoyClient` + серверный эндпоинт в `roy.py` + чтение страницей сайта»; берётся как
форма, семантика `roy_pool` — нет (2.3).

## Стадия 3. Contract Extraction

Формат: `Требование → Контракт → Enforcement → Verification`. Нумерация: контракты **P-**, находки **PA-**,
тесты **PT-** (не пересекаются с C-/A-/T- других спек).

| № | Контракт | Enforcement (планируемый) | Verification |
|---|---|---|---|
| P-01 | Публикация вызывается только при **успешном списании 10◆ и успешном OCR** (`coords_ok`); иначе не вызывается. **Это контракт штатного клиента, а не серверный инвариант: сервер факта списания не проверяет (PA-5).** Порядок остальных шагов цепочки (показ находки; OCR относительно списания) определяют Части A/B и спека №2; цепочка владельца — в Стадии 1. | ветка consumer'а: `publish` вызывается только при `charged is True and coords_ok is True` | PT-01, PT-02 |
| P-02 | Контракт данных. **Запрос `POST`:** `hwid` (только для проверки пользователя), `kingdom` (строгий int > 0), `x`, `y` (строгий int, без приведения типов, в диапазоне 32-битного `Integer` колонки: −2147483648…2147483647; иных бизнес-ограничений на X/Y нет). **В БД:** `kingdom`, `x`, `y`, `reporter_hwid` (внутренний, наружу не отдаётся), `found_at` (ставит сервер). **Публично (`GET`):** ровно `kingdom`, `x`, `y`, `found_at`. Не передаются и не хранятся: изображение, `percent`, `conf`, `box`. | pydantic-схема `ScoutFindRequest` (`StrictInt` + `Field(ge=-2147483648, le=2147483647)` для x, y; `StrictInt` + `Field(ge=1)` для kingdom — проверено на pydantic 2.13.1: обычный `int` приводит `"123"`, `1.0`, `True`, `StrictInt` их отклоняет) + публичная схема ответа без hwid | PT-03, PT-04 |
| P-03 | Отдельный механизм: новая таблица `roy_scout_finds` и эндпоинт `POST /roy/scout-find`. **Не** используются `/roy/report`, `roy_pool`, `_report_rate`, `_broadcast(pool_updated=True)`, `send_telegram_alert`. | отдельная функция в `server/roy.py`, отдельная модель в `server/models.py`, alembic-миграция | PT-05 |
| P-04 | Для публикации 2.0 нет rate limit и нет дедупликации (решение владельца). Каждый **валидный авторизованный** вызов (P-05) = новая строка. | в коде эндпоинта нет лимитера и нет `UNIQUE` на (kingdom,x,y) | PT-06 |
| P-05 | Валидация: `hwid` существует и не забанен (404/403 как в `/use_credit`); `kingdom` — строгий int > 0; `x`, `y` — строгие int в диапазоне `Integer` (P-02). | проверка пользователя как `server/main.py:324-332` (длина hwid ограничена существованием пользователя: `User.hwid` `String(16)`, 2.6a) + `kingdom: StrictInt` + `Field(ge=1)`; `x`, `y`: `StrictInt` + диапазон P-02 | PT-07, PT-08 |
| P-06 | Клиент: если `_get_roy_kingdom()` вернул `0`, публикация **не выполняется** (сервер такое королевство не показывает, `roy.py:130`); причина пишется в лог сессии. Списание при этом уже состоялось (P-01) — находка остаётся в `scout_results.jsonl`. **Значение `kingdom` фиксируется в результате находки в момент её создания и передаётся в `report_scout_find` вместе с результатом; публикация НЕ перечитывает `_get_roy_kingdom()` из GUI** (иначе смена поля между находкой и публикацией даёт чужое королевство). Значение хранится в результате находки в памяти consumer'а, в `scout_results.jsonl` не добавляется (правка Части A вне этой спеки). Точка фиксации — Часть B [→ Часть B], которая обязана реализовать последовательность: захват K → создание результата → результат несёт этот K → GUI после этого не читается. | проверка `kingdom > 0` в клиентском методе перед запросом; `kingdom` — параметр `report_scout_find`, читаемый consumer'ом один раз при создании результата | PT-09; PT-18 (тест плана consumer'а) |
| P-07 | Публикация не блокирует consumer: вызов идёт в отдельном треде с `_TIMEOUT = 5`, как `RoyClient.report()` (2.5); consumer сразу берёт следующий кадр. | новый метод `RoyClient.report_scout_find()` по образцу `report()` | PT-10 |
| P-08 | Сбой публикации (сеть, 4xx/5xx, таймаут) НЕ отменяет списание; **после успешной фиксации результата в локальном журнале сбой публикации не приводит к потере результата обработки** (до завершения `fsync` записи результат не гарантирован). **Контракт к consumer'у [→ Часть B]:** результат находки фиксируется локально (строка `scout_results.jsonl`, Часть A §4.6) **до запуска публикации** — иначе при падении процесса возможно списание без локального следа. **`scout_results.jsonl` — журнал результата обработки, а не журнал публикации:** после рестарта публикация не восстанавливается, `kingdom` в JSONL отсутствует сознательно (non-goal), для повторной публикации K не сохраняется. Повтор публикации не делается (non-goal), сбой пишется в лог сессии. | `try/except` вокруг запроса, исключение не выходит из треда; consumer: запись строки журнала с `fsync` предшествует вызову `report_scout_find` | PT-11; PT-17 (тест плана consumer'а) |
| P-09 | Чтение сайтом: публичный `GET /roy/scout-finds` (без авторизации) возвращает список `{kingdom, x, y, found_at}`, новые первыми; hwid в ответе отсутствует. Показываются только находки не старше **20 минут** от публикации (`found_at > now - 20 мин`, P-13). | отдельная функция в `roy.py` с явным `select` только этих колонок | PT-12 |
| P-10 | Сайт: на `RoyPage.jsx` добавляется блок «Находки Биржи 2.0» (список из P-09), данные грузятся при открытии страницы и кнопкой «Обновить». Существующий список королевств и SSE не меняются. | отдельный `useEffect`/`fetch`, отдельное состояние | PT-13 (ручная проверка страницы) |
| P-11 | Текст на сайте — только факты: «Примерное расположение: позиция экрана бота в момент кадра, не точные координаты биржи». Языки — те же, что у страницы (RU/EN). | строки в `RoyPage.jsx` по образцу `isRu` | PT-13 |
| P-12 | Поведение Биржи 1.0 не изменяется: `/roy/report`, `/roy/pool`, `roy_pool`, `_report_rate`, SSE-поток, `RoyClient.report/get_pool` работают как прежде. | добавляются только новые символы; существующие не редактируются | PT-05, PT-14 |
| P-13 | Срок жизни находки на сайте — **20 минут** (решение владельца, как у пула 1.0), считаются от серверного `found_at`, то есть **от момента публикации (POST), а не от момента захвата кадра или детекции YOLO**. Константа `SCOUT_FIND_TTL_MIN = 20` — **своя**, не `POOL_TTL_MIN` (правка общей константы задела бы 1.0). Чтение показывает только строки моложе срока (`found_at > now − 20 мин`); каждая вставка в `scout-find` удаляет строки, возраст которых не меньше срока (`found_at <= now − 20 мин`) (общий `_cleanup_loop` не трогаем). | фильтр в `select` + `delete` в той же транзакции вставки | PT-15, PT-16 |

Неявные границы контрактов: P-09/P-10 — единственное, что видит посторонний посетитель сайта; всё, что не
перечислено в P-02, наружу не выходит.

## Стадия 4. Design

**4.1 Данные.** Таблица `roy_scout_finds`: `id` (PK), `kingdom` (Integer, index), `x`, `y` (Integer),
`reporter_hwid` (String(16), для админской трассировки, наружу не отдаётся), `found_at`
(`TIMESTAMP(timezone=True)`, `server_default=func.now()`, index). Типы — по образцу `RoyPool`
(`models.py:279-289`). Без `UNIQUE` (P-04).

**4.2 Серверные эндпоинты (в `server/roy.py`, router `/roy`).**
- `POST /roy/scout-find` — тело `{hwid, kingdom, x, y}`. Шаги: найти пользователя → 404/403 (P-05) → удалить строки
  с `found_at <= now − 20 мин` (P-13) → вставка строки → `commit` → `{"success": True}`. Одна транзакция, `async with db.begin()` как в остальном файле.
  SSE не трогается.
- `GET /roy/scout-finds` — публичный список (P-09).

**4.3 Клиент.** В `roy/roy_client.py` — новый метод `report_scout_find(kingdom, x, y)`; существующие методы не
меняются. Вызывается consumer'ом 2.0 после OCR (Часть B определит точку вызова).

**4.4 Сайт.** Новый блок под списком королевств в `RoyPage.jsx`: заголовок, поясняющая строка (P-11), список
`K · X · Y · время`, кнопка «Обновить», пустое состояние.

**4.5 Внешние вызовы и отказы** (A — ошибка, B — зависание, C — повтор, D — дважды, E — частично, F — параллельно):

| Вызов | A | B | C | D | E | F |
|---|---|---|---|---|---|---|
| `POST /roy/scout-find` из клиента | лог, находка остаётся локально (P-08) | `timeout=5` `requests` (5 с на подключение и 5 с на чтение отдельно, суммарного лимита нет) — тред не гарантированно завершается за 5 с, consumer при этом не блокируется (P-07, PA-7) | повтора нет (non-goal) | две строки — решение владельца (P-04) | транзакция БД атомарна: строка есть или нет | два процесса 2.0 одного hwid — две независимые вставки, конфликтов нет |
| `GET /roy/scout-finds` с сайта | блок показывает пустое состояние/ошибку | браузерный таймаут по умолчанию | безопасен (read-only) | безопасен | — | безопасен |

**4.6 Состояния публикации одной находки** (после успешного списания и успешного OCR; порядок «показ → OCR»
относительно списания определяют Части A/B и здесь не фиксируется).
`READY_TO_PUBLISH → PUBLISH_SENT → {PUBLISHED | PUBLISH_FAILED}`; пропуски: `SKIPPED_NO_KINGDOM |
SKIPPED_NOT_CHARGED | SKIPPED_NO_COORDS`. Переходы вперёд необратимы. Краш процесса между списанием и
`PUBLISH_SENT`: кредит списан, `scout_results.jsonl` уже содержит запись к моменту списания (C-17(c) спеки №2) и
обязательно содержит её до запуска публикации (P-08), публикация не выполнена и **при рестарте не доигрывается** (нет
журнала повторов — non-goal).

**4.7 Non-goals (явно вне документа).** Восстановление публикации после рестарта; хранение `kingdom` в `scout_results.jsonl`; серверная привязка публикации к списанию (PA-5); Публикация изображения/кропа мини-карты (в `CLAUDE.md` запрещено
хранить файлы на GCP; в цепочке владельца только координаты); повтор неудачной публикации; дедупликация;
live-обновление сайта через SSE; уведомления/звук у других пользователей; сортировка/фильтры на сайте;
правка Части A (`scout_results.jsonl` не получает новых полей).

## Стадия 5. Adversarial Review (формула `claim → mechanism → bypass → result`)

- **PA-1. «Посторонний не увидит hwid».** Механизм: явный `select` колонок (P-09). Обход: `SELECT *` в
  будущей правке. Результат: тест PT-04 падает, если в JSON ответа есть `hwid`/`reporter_hwid`.
- **PA-2. «Через x/y нельзя внедрить разметку».** Механизм: только `StrictInt` (pydantic) + React экранирует текст.
  Обход: строка (в т. ч. числовая `"123"`), float или bool вместо int → 422 (`StrictInt`; без него обычный `int` приводил бы `"123"`, `1.0`, `True` к числу), строка в БД не попадёт. Результат: безопасно; PT-03.
- **PA-3. Флуд публичной страницы.** Механизм: только hwid существующего небаненого пользователя (P-05).
  Обход: любой зарегистрированный (в т.ч. бесплатный триал) может слать без лимита, а server не связывает
  публикацию со списанием. **Контрпример найден и ПРИНЯТ:** rate limit и дедуп запрещены решением владельца
  (P-04); ограничение — бан пользователя. Это осознанный остаточный риск, не закрытый механизмом.
- **PA-4. Один и тот же объект в нескольких кадрах.** Без дедупа список содержит повторы одной биржи.
  Принято владельцем (решение 3). Не «баг», ожидаемое поведение.
- **PA-5. Принятый остаточный риск: запись в РОЙ без списания.** Штатный клиент вызывает публикацию только после успешного списания (P-01), но `POST /roy/scout-find` принимает `hwid, kingdom, x, y`, а сервер не имеет связи этого запроса с конкретным успешным списанием, поэтому модифицированный клиент может вызвать эндпоинт напрямую. Серверная привязка публикации к операции списания (payment-token и т. п.) в этой версии сознательно не реализуется (Stage 1 п. 7). Риск принят владельцем 2026-09-18 («остаточные риски принимаем; дедупликацию и лимит не добавляем»). Ущерб — запись на публичной странице, живёт 20 минут (P-13).
- **PA-6. Рост таблицы.** Без очистки `roy_scout_finds` растёт бесконечно; БД сейчас 38 МБ, e2-micro (проверено
  сегодня). Механизм очистки — P-13 (срок 20 мин, решение владельца); очистка при вставке — в самой функции
  `scout-find`, а **не** в общем `_cleanup_loop` (`roy.py:168`), чтобы не менять поведение 1.0.
- **PA-7. Зависший сервер и число тредов.** Механизм: отдельный тред + `timeout=5` (P-07) — consumer не блокируется. Но `timeout=5` в `requests` = 5 с на подключение и отдельно 5 с на чтение (проверено: суммарного лимита `total` нет), поэтому тред **не гарантированно** завершается за 5 с, а число одновременно живущих тредов при потоке находок **не ограничено**. Это допущение, а не мера защиты: допустимо при ожидаемо низкой частоте находок бирж; возможная защита (пул/семафор) — новая механика, вне scope.
- **PA-8. Смена королевства в GUI между находкой и публикацией — закрыто контрактом P-06.** `kingdom` фиксируется
  в результате находки при его создании, публикация GUI не перечитывает (PT-18). **Остаточное допущение:** очередь неограничена,
  поэтому между съёмкой кадра и созданием результата значение в GUI ещё может смениться. **Гарантируется только неизменность K после создания результата, но не то, что K соответствует моменту захвата конкретного кадра.** Это окно остаётся сознательным допущением этой спеки; закрытие его фиксацией K при старте сессии в неё не входит [→ Часть A/B]. Номер вводится участником
  добровольно и не проверяется: неверно указанное королевство даст находку под чужим номером (сервер не отличает).
- **PA-9. Изоляция 1.0.** Механизм: только новые символы (P-12). Обход: правка общего `_broadcast`. Результат:
  запрещена P-03; PT-14 проверяет, что `GET /roy/pool` и `roy_pool` не меняются после публикации 2.0.
- **PA-10. Порядок P-01 при таймауте ответа списания.** Если ответ `/use_credit` потерян, `charged` неизвестен →
  публикация **не выполняется** (`charged is True` — строгая проверка). Результат: находка не выложена, кредит
  мог списаться — вопрос потерянного ответа разобран в спеке монетизации (§2.8).
- **PA-11. X/Y вне 32-битного `Integer` — закрыто контрактом P-02.** Колонка `Integer` (Postgres, 32 бита), поэтому
  схема ограничивает x, y тем же диапазоном: значение вне диапазона → 422 до обращения к БД, а не 500 из БД.
  Тест PT-03 проверяет обе границы диапазона и оба выхода за диапазон.

## Стадия 6. Test Matrix

Серверные тесты — `server/tests/test_roy_scout.py`; клиентские — рядом с существующими тестами клиента
(точное место определит план); ручная проверка сайта — PT-13.

| Тест | Инвариант | Normal | Boundary | Failure | Concurrent | Recovery |
|---|---|---|---|---|---|---|
| PT-01 `publish_not_called_when_charge_failed` | P-01 | — | ответ `low_credits` | 402/сеть | — | — |
| PT-02 `publish_not_called_without_coords` | P-01 | — | `coords_ok=false` | — | — | — |
| PT-03 `scout_find_validates_xy_type_and_integer_range` | P-02, PA-2, PA-11 | int → 200 | `0`, отрицательные, `2147483647` и `-2147483648` → 200; `2147483648` и `-2147483649` → 422 | строка (в т. ч. `"123"`), float, bool → 422 | — | — |
| PT-04 `scout_finds_response_has_no_hwid` | P-02, PA-1 | поля ровно {kingdom,x,y,found_at} | пустой список | — | — | — |
| PT-05 `scout_find_does_not_touch_roy_pool` | P-03, P-12 | `roy_pool` count не меняется | — | — | — | — |
| PT-06 `scout_find_no_rate_limit_no_dedup` | P-04 | 2 запроса подряд с одинаковыми K/X/Y → 2 строки | <1 с между запросами | — | не воспроизводится на SQLite (`StaticPool`, одно соединение); общего состояния между вызовами нет по построению | — |
| PT-07 `scout_find_unknown_hwid_404` / `banned_403` / `hwid_longer_than_16_404` | P-05, 2.6a | существующий → 200 | hwid длиннее 16 символов → 404, вставки нет | 404, 403 | — | — |
| PT-08 `scout_find_kingdom_lt_1_rejected` | P-05 | K=1 → 200 | K=0, K=-1 → 422 | `"1"`, `1.0`, `true` → 422 (строгий int) | — | — |
| PT-09 `client_skips_publish_when_kingdom_zero` | P-06 | K>0 → запрос | K=0 → нет запроса | — | — | лог пишется |
| PT-10 `client_publish_does_not_block_consumer` | P-07 | — | сервер отвечает 5 с | сервер висит | — | consumer не ждёт завершения HTTP-вызова; при штатном завершении `requests` тред завершается после ответа, исключения или timeout (жёсткого предела в 5 с нет — PA-7) |
| PT-11 `client_publish_failure_is_swallowed` | P-08 | — | — | ConnectionError, 500, таймаут | — | consumer продолжает |
| PT-12 `scout_finds_lists_newest_first` | P-09 | порядок по `found_at` desc | 0 записей | — | — | — |
| PT-13 ручная проверка страницы | P-10, P-11 | блок виден, тексты RU/EN | пустой список | сервер недоступен | — | «Обновить» перечитывает |
| PT-14 `roy_report_and_pool_unchanged` | P-12, PA-9 | регрессия существующих `test_roy.py` зелёные | — | — | — | — |
| PT-15 `scout_finds_excludes_older_than_ttl` | P-13 | запись 19 мин 59 с видна | ровно 20 мин 00 с и 20 мин 01 с — не видны; часы `roy.datetime` зафиксированы (frozen clock, образец `test_roy.py`), граница детерминирована | — | — | — |
| PT-16 `scout_find_insert_deletes_expired` | P-13, PA-6 | запись возрастом ≥ 20 мин удалена при вставке | — | — | — | — |
| PT-17 `consumer_writes_journal_before_publish` (план consumer'а, Часть A/B) | P-08 | строка журнала записана до вызова `report_scout_find` | журнал не записан → списания нет (C-17(c)) → публикации нет (P-01) | — | — | — |
| PT-18 `consumer_publishes_kingdom_captured_at_find` (план consumer'а, Часть A/B) | P-06, PA-8 | опубликован K, зафиксированный при создании результата | GUI сменили на другой K между находкой и публикацией → публикуется прежний K | — | — | — |

## Стадия 7. Self-Audit

Триаж слов-триггеров (`ровно / всегда / никогда / не ... нельзя / atomic`):
- «Публично ровно {kingdom,x,y,found_at}» (P-02) — категория (a), механизм: явный `select` + PT-04.
- «Одна транзакция» (4.2) — (a), `async with db.begin()`, паттерн `roy.py:265`.
- «Поведение 1.0 не изменяется» (P-12) — (a); runtime-enforcement нет, поведение доказывается регрессионными тестами PT-14 + дисциплиной изменений (только добавления символов); проверка `git diff` плана на отсутствие удалений — процессная проверка разработки, а не enforcement контракта.
- «Повтора нет» (P-08) — (b), non-goal, не требование.
- «Не блокирует consumer» (P-07) — (a), PT-10.

Найдено при самопроверке (содержательное): (1) PA-5 — серверная связь публикации со списанием отсутствует и сознательно не реализуется (Stage 1 п. 7); это принятый остаточный риск этой версии; (2) очистку таблицы нельзя класть в общий `_cleanup_loop` —
изменила бы поведение 1.0; (3) `kingdom = 0` в GUI → публикация молча не выполнится; вынесено в P-06 как
контракт с логом, а не «на усмотрение».

Requirements coverage: цепочка владельца — каждый шаг после «показать пользователю» покрыт (P-01, P-06-P-10);
шаги «списать» и «показать» — в спеке монетизации и Части A.

## Стадия 9. Final Gate

Проверено: каждое утверждение о существующем коде — из файлов, открытых лично в этой сессии (`server/roy.py`
1-300, `server/models.py` RoyPool, `roy/roy_client.py`, `web/src/pages/RoyPage.jsx`, фрагменты `main.py`,
`server/main.py` 288-357, `server/models.py` User/RoyPool, `auth.py:30-36`). Каждый контракт имеет описанный механизм реализации и verification; где runtime-enforcement не применяется (P-12 — поведение 1.0 доказывается регрессионными тестами и дисциплиной изменений; P-01 — контракт штатного клиента, PA-5), это указано явно. Stage 8 — «не запрошен владельцем».

### Открытые вопросы владельцу

Нет. **ОВ-1 закрыт владельцем 2026-09-18:** срок показа находки — 20 минут, как пул 1.0; к окончанию ивента не привязывается, «N часов» не вводится.

## Что осталось непроверенным

1. Применение миграции на GCP: текущая head-ревизия (`b1a2c3k4s5e6`) определена скриптом по `server/alembic/versions/`, но Alembic на GCP по памяти проекта падает без `DATABASE_URL` — таблица создаётся вручную через `psql`; порядок выкатки определит план (задача деплоя, гейт владельца).
2. Полное содержимое `server/roy.py` после строки 300 и `GET /roy/kingdoms` (:382-390) — не читал; на них спека
   не опирается, кроме факта наличия по docstring (:11).
3. Ревизия серверного кода на GCP соответствует репозиторию — не проверялась.
4. Как именно consumer 2.0 будет вызывать публикацию — за Частью B (конкурентность). Порядок «журнал → публикация»
   зафиксирован контрактом P-08 и **передаётся в Часть B [→ Часть B]**.
5. Nginx `client_max_body_size` на маленький JSON не влияет, но конфиг не читался.
