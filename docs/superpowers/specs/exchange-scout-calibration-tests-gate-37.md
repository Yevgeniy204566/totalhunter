# Калибровка и рекомендуемые значения — Стадия 6 (Test Matrix), 7 (Self-Audit), 9 (Final Gate)

> Индекс и статус: [`exchange-scout-calibration-context-32.md`](exchange-scout-calibration-context-32.md).
> Части: [32](exchange-scout-calibration-context-32.md) (Context) ·
> [33](exchange-scout-calibration-archaeology-33.md) (Code Archaeology) ·
> [34](exchange-scout-calibration-contracts-34.md) (Contract Extraction) ·
> [35](exchange-scout-calibration-design-35.md) (Design) ·
> [36](exchange-scout-calibration-adversarial-36.md) (Adversarial Review) · **37 (этот файл)**.
> Stage 8 (External Agent Review) не запрошен владельцем — золотое правило `CLAUDE.md`/
> `docs/РАБОТА-С-ДОКУМЕНТАМИ.md`: не блокирует Final Gate.

---

## Стадия 6. Test Matrix

Новый тестовый файл `test_calibration_panel.py`. GUI-тесты работают через `tkinter`/`customtkinter` test
harness проекта (по образцу существующих GUI-тестов, если такие есть — иначе через прямую проверку
состояния виджетов без реального рендера экрана). Тесты C-01…C-06 — Раздел 1; C-07…C-10 — Раздел 2;
T-11…T-13 — находки Stage 5 (A-1…A-3).

| # | Invariant | Normal | Boundary | Failure | Recovery | Test |
|---|---|---|---|---|---|---|
| T-01 | C-01 | список показывает все 12 целей, ровно один вход | — | второй независимый триггер калибровки A/Б отсутствует в коде | — | `test_single_calibration_entry_point` |
| T-02 | C-02 | у каждой из 12 целей `kind` ∈ {точка, OCR-область, смещение-к-YOLO} | — | цель без `kind` или с посторонним значением — тест падает | — | `test_all_targets_have_valid_kind` |
| T-03 | C-03 | для каждого `kind` — ровно один действующий визуальный путь | старый путь (`calibration_ui._show_red_dot`) не используется параллельно с новым | — | — | `test_one_rendering_path_per_kind` |
| T-04 | C-04 | тип считывается независимо от темы (`chest_collect` — точка, `chest_sender`/`chest_type` — область, все три — тема «сундуки») | — | — | — | `test_theme_and_kind_are_independent_channels` |
| T-05 | C-05 | ввод через новую панель даёт те же значения в `coord_manager`, что дал бы старый раздельный UI | — | — | — | `test_new_panel_preserves_coordinate_model` |
| T-06 | C-06 | ссылка на видео — гиперссылка, не встроенный плеер | пустой URL → элемент скрыт/неактивен, не битая ссылка | — | — | `test_video_link_hidden_until_url_set` |
| T-07 | C-07 | все 19 известных id слайдеров имеют запись рекомендуемого значения | — | слайдер без записи — тест падает | — | `test_all_19_sliders_have_recommended_value` |
| T-08 | C-08 | 19 значений совпадают дословно с таблицей файла 32, включая `march_max_sec=120` (owner override, не 202) | — | — | — | `test_recommended_values_match_owner_table` |
| T-09 | C-09 | зелёная зона есть, кнопки «вернуть рекомендуемое» нет ни у одного из 19 | — | — | — | `test_no_restore_recommended_button` |
| T-10 | C-10 | ширина зоны ≈ `±5%` диапазона (`(to-from)*0.05`), цвет == `MD3["green_btn"]` | значение на самой границе `from`/`to` — зона обрезается по границе слайдера, не выходит за неё | — | — | `test_green_zone_width_and_color` |
| T-11 | A-1 | не существует двух одновременно активных путей, независимо пишущих значение одной цели | — | если найден дублирующий путь — тест падает (ревью кода/структуры на PLAN) | — | `test_no_dual_write_path_per_target` |
| T-12 | A-2 | геометрия зелёной зоны соответствует текущей геометрии слайдера | после изменения размера окна/контейнера геометрия зоны пересчитана | геометрия не обновилась после resize — тест падает | resize → пересчёт | `test_green_zone_geometry_updates_on_resize` |
| T-13 | A-3 | `base_pos_fn` вызывается заново при каждом рендере/выборе цели | после повторной калибровки `ref_a`/`ref_b` (новые `scale_x`/`scale_y`) превью офсетной точки отражает новую калибровку, не старую | закэшированное числовое значение вместо вызова функции — тест падает | повторная калибровка → пересчёт | `test_base_pos_fn_reflects_latest_calibration` |

---

## Стадия 7. Self-Audit

Грепнул собственный текст файлов 32–36 на `MUST/ОБЯЗАН/ОБЯЗАТЕЛЬНО/ВСЕГДА/НИКОГДА/только/единственн/
атомарн/уникальн/гарантир`. Триаж: **(a)** реальный contract/safety claim → нужен механизм; **(b)** проза/
пример; **(c)** уже разобрано в другом месте документа.

| Формулировка | Кат. | Механизм / разбор |
|---|---|---|
| «единый пользовательский вход, без второго независимого триггера» | (a) | C-01, единый список 12 целей; T-01 |
| «тип — явное поле данных, ровно три значения» | (a) | C-02, реестр 4.1; T-02 |
| «единый визуальный путь на тип» | (a) | C-03, 4.3; T-03 |
| «тип и тема — независимые каналы» | (a) | C-04, 4.4; T-04 |
| «координатная модель не меняется» | (a) | C-05, 4.5; T-05, доказано чтением `coord_manager.py` (файл 33) |
| «видео — только гиперссылка» | (a) | C-06, 4.6; T-06 |
| «все 19 без исключений» | (a) | C-07, 4.7; T-07 |
| «19 значений — данные, не вычисляются» | (a) | C-08, значения из реального профиля владельца (файл 32), один явный override; T-08 |
| «только зелёная зона, без кнопки» | (a) | C-09, 4.7; T-09 |
| «ширина зоны провизорна, делегирована владельцем» | (c) | C-10 — уже явно помечено как provisional, не абсолютная гарантия; T-10 проверяет текущее провизорное значение |
| «`base_pos_fn` не кэшируется» | (a) | A-3/Design 4.1, реализационный инвариант для PLAN; T-13 |
| «нет двух независимых источников данных одной цели» | (a) | A-1, инвариант миграции; T-11 |
| «геометрия зоны обязана следовать за геометрией слайдера» | (a) | A-2/Design 4.7; T-12 |

**Находок самопроверки: 0.** Все обязывающие формулировки уже имеют контракт/находку и тест — новых
пробелов при повторном проходе не обнаружено.

---

## Стадия 9. Final Gate

| Критерий протокола | Статус |
|---|---|
| Нет непроверенных утверждений о существующем коде | ✅ все ссылки на `main.py`/`coord_manager.py`/`calibration_ui.py`/`chest_reader.py` — с номером строки, прочитаны лично в этой сессии (файл 33, 35, 36) |
| Каждый safety claim имеет доказательство или контрпример | ✅ Стадия 5, A-1…A-5, формула `claim → mechanism → bypass → result` |
| Concurrency semantics проверены | ➖ не применимо — калибровка синхронна, GUI-действие без фоновых тредов (файл 32, «Что эта спека НЕ является») |
| Retry/resource budget проверен контрпримером | ➖ не применимо — нет сетевых вызовов/повторов в этой спеке |
| Важные требования не живут только в прозе | ✅ 10 контрактов C-01…C-10, у каждого механизм и тест |
| Каждый критический инвариант имеет названный планируемый тест | ✅ T-01…T-13 |
| Stage 8 | ➖ не запрошен владельцем — не блокирует Final Gate |
| Открытые вопросы не спрятаны | ✅ C-10 (ширина зоны) — явно provisional, а не выдано за финальное решение; реальный URL видео (C-06) — явно отложен до предоставления владельцем; A-4 (контейнерная иерархия) — явно передано как первый шаг PLAN |
| Лимит раундов правок | ⚠️ отменён — правили до одобрения GPT, счётчик не является тормозом |

**Статус документа: ✅ Final Gate пройден 2026-09-19.** Оба раздела калибровки (единая панель 12 целей;
рекомендуемые значения 19 слайдеров) прошли Стадии 1–7, 9 полностью. Готово к PLAN.

**Что осталось непроверенным (честно, не спрятано):**
1. Реальный URL видео — владелец предоставит позже (C-06, изначально закрыто как «отдельно»).
2. Точная контейнерная иерархия существующих виджетов вкладки калибровки (A-4) — первый шаг PLAN, не
   проверено в этой сессии.
3. Ширина/цвет зелёной зоны (C-10) — провизорные значения, владелец может скорректировать при первом
   реальном просмотре без нового раунда Contract Extraction.
