# Биржа 2.0 (Exchange Scout) — ЧАСТЬ B: конкурентность — Стадия 4 (Design), блок 1/3 — Кластер D

> Индекс и статус: [`exchange-scout-part-b-context-29.md`](exchange-scout-part-b-context-29.md).
> Части: [29](exchange-scout-part-b-context-29.md) (Context) · [30](exchange-scout-part-b-archaeology-30.md)
> (Code Archaeology) · [31](exchange-scout-part-b-contracts-31.md) (Contract Extraction) · Design (Стадия
> 4) разбит по кластерам после превышения ≤300 строк/файл (решение владельца, раунд 7): **44 (этот файл —
> §4.0, кластер D)** · [46](exchange-scout-part-b-design-46.md) (кластеры A/B/C) ·
> [47](exchange-scout-part-b-design-47.md) (кластер E, числа §4.6, связь с калибровкой §4.7, итог).
> **Статус:** ЧЕРНОВИК на ревью владельца, раунд 7 (разбивка на файлы, содержание раунда 6 без
> изменений по существу). Не начат Stage 5 (Adversarial Review) — начинается только после явного «да»
> по всем трём файлам Design. Код не пишется до Final Gate всей серии Части B.

---

## 4.0 Структурное решение

Все 5 кластеров (файл 31, §3.8) укладываются в **один комплект Design из 3 файлов** (раунд 7: изначально
был один файл, разбит после превышения объёма и накопления ошибок при ревью — решение владельца), не
5-6 отдельных документов: три кластера (D, A, B) решаются независимо простыми механизмами, а два
оставшихся (C, E) оказываются **прямым следствием** решений по D и B, а не отдельной архитектурой — см.
§4.4/4.5 (файлы 46/47). Оценка файла 31 §3.9 («1-2 документа по 5-6 файлов») была верхней границей, не
обязательством — фактический объём меньше.

Порядок изложения — по графу зависимостей (файл 31, §3.8): **D → A → B → C → E** (C и E последними,
потому что оба используют результат более ранних решений). Разбивка на файлы следует этому же порядку:
блок 1 (этот файл) — D, блок 2 (файл 46) — A/B/C, блок 3 (файл 47) — E + числа + итог.

---

## 4.1 Кластер D — устройство очереди при перезапуске

**Решение: единая очередь, не per-sid.** Consumer не ограничен своей текущей `pending/<sid>/` — при
каждой итерации он видит **всё дерево `pending/*/*.jpg`** (все сессии, старые и текущую), не только
папку своего `sid`. Producer по-прежнему пишет только в свою собственную `pending/<sid>/` (C-01 не
меняется) — меняется только то, откуда **читает** consumer.

**Почему не per-sid.** Часть A уже явно намекает на это направление (файл 14, §4.2: «кадр либо будет
обработан (своим или **продолжающим** consumer'ом)») — строгая модель «только свой sid» означала бы,
что кадры, снятые за секунды до краша, гарантированно **никогда не обрабатываются** (сидят в `pending/`
до TTL и удаляются нетронутыми) — это прямая потеря потенциальных находок без файловой необходимости.
**Уточнение по ревью владельца (раунд 8) — «бесплатно» было неточным словом:** единая очередь устраняет
эту потерю без введения отдельного механизма восстановления (новый consumer любого процесса подбирает
оставшийся `pending`-кадр сам, тем же кодом, что и свежие кадры) — но не буквально бесплатно: именно
из-за расширенного скана и был найден producer/consumer race на JPEG (§4.1.2 ниже). Решение не имеет
скрытой цены, но имеет явную, тут же закрытую.

**Механизм выбора следующего кадра.** Кадры выбираются в порядке возрастания `mtime` — эвристика
приоритета старых файлов, не гарантия строгого FIFO (`mtime` имеет ограниченную точность, несколько
файлов могут получить одинаковое значение, скан не атомарен относительно новых кадров, дописываемых
producer'ом). Внутри одного процесса consumer один (Часть A, таблица 4.2) — обход всего дерева одним
потоком не создаёт новой конкуренции сам по себе.

**Что это НЕ меняет.** `pending/<sid>/` как структура папок (C-01) не убирается — она остаётся нужна для
изоляции producer'а (C-06, уникальность имён кадров внутри сессии). Меняется только область просмотра
consumer'а: с «своя папка» на «всё дерево».

### 4.1.1 Отдельный вопрос — crash МЕЖДУ `os.replace` и завершением обработки (найдено ревью владельца)

В реальной архитектуре нет промежуточной папки «processing» — `os.replace` (C-08) атомарно переносит
кадр **напрямую** `pending/<sid>/` → `found/<sid>/`, без третьего состояния. Вопрос: что происходит с
кадром, если процесс упал **после** этого переноса, но **до** завершения последовательности «журнал →
списание → публикация» (C-03)?

**Проверено по Части A: этот кадр НЕ покрыт §4.1 выше и вообще не покрыт TTL.** C-02 (TTL, 30 минут)
явно относится только к `pending/`; `found/` нигде в Части A не получает TTL или другого автоматического
механизма — единственный упомянутый способ его очистки — «пользователь может удалить `found/` вручную в
любой момент, но это не штатный путь» (C-03). Значит такой осиротевший кадр в `found/<sid>/` от
крашнувшейся сессии — это уже существующий, не новый в Части B, пробел Части A, который я предлагаю
явно унаследовать, а не молча закрыть.

**Решение Части B: НЕ вводить авто-возобновление обработки для `found/`.** Осиротевший кадр в
`found/<sid>/` крашнувшейся сессии остаётся на диске нетронутым — тем же режимом, что и `errors/`
(диагностический остаток, ручная очистка), а не как элемент recoverable-очереди. Причина не вводить
авто-resume: чтобы безопасно продолжить с этого места, consumer должен был бы уметь ИДЕМПОТЕНТНО
определить, была ли уже сделана запись в `scout_results.jsonl`/списание/публикация для этого конкретного
кадра — а текущая схема журнала (C-09/4.6, файл 14) не хранит признак «попытка оплаты была предпринята»,
только факт совершившейся находки. Введение такого признака — это расширение денежной схемы журнала
(Часть A-М), не архитектура конкурентности; смешивать это в Part B значило бы тихо брать на себя чужой
scope.

**Уточнение по ревью владельца (раунд 6) — шаги нельзя называть «атомарными».** Порядок «журнал →
списание → публикация» (C-03) фиксирован, но ни один из трёх шагов не является атомарным сам по себе:
- журнал — Часть A **сама** признаёт возможность обрезанной строки при crash во время записи (C-09,
  тест T-13: «обрезанная последняя строка читается без падения ридера») — то есть журнал может оказаться
  в промежуточном, а не только «есть/нет» состоянии на уровне байт (хотя для целей этой таблицы важен
  более грубый факт — успела ли строка стать частью файла до `fsync`, а не побайтовая целостность);
  publisher/будущий читатель `scout_results.jsonl` обязан не падать на такой строке, что уже покрыто C-09;
- списание и публикация — сетевые HTTP-вызовы; сам факт на сервере в конце концов бинарен (запрос либо
  обработан, либо нет), но **это только конечный, а не мгновенный результат** — во время самого запроса
  локальный процесс не имеет доступа к промежуточному состоянию вообще, только к исходу после ответа (или
  к его отсутствию при crash до ответа).

**Уточнение по ревью владельца (раунд 8) — «точки между завершёнными шагами» было неточно для третьей
строки.** Таблица ниже фиксирует состояния и точки остановки в последовательности «журнал → списание →
публикация»: где шаг уже завершён, это указано явно (да/нет); третья строка — исключение, она находится
**внутри** сетевого шага списания (запрос отправлен, ответ ещё не получен), а не между двумя завершёнными
шагами — для неё столбец показывает не факт, а то, что локальный результат в этот момент неизвестен (см.
оговорку сразу под таблицей). Список строк закрыт по фиксированному порядку C-03, не открыт.

| Crash произошёл | Журнал | Списание | Публикация |
|---|---|---|---|
| сразу после `os.replace`, до начала записи журнала | нет | нет | нет |
| после того как строка журнала зафиксирована (`fsync` вернул успех), до отправки запроса на списание | есть | нет | нет |
| **внутри** сетевого шага списания — запрос отправлен, ответ ещё не получен | есть | **см. оговорку ниже — локально неизвестно** | нет |
| после подтверждённого успешного списания, до публикации | есть | да | нет |
| после публикации (последний шаг) | есть | да | да |

Пять строк фиксируют ключевые точки остановки в этой последовательности при фиксированном порядке шагов,
не открытый список; они не утверждают, что переход между соседними строками сам по себе мгновенен или
неделим — третья строка специально показана как исключение из «между завершённого» именно потому, что
она внутри незавершённого шага.

**Число осиротевших кадров зависит от числа процессов, не «ровно один».** Внутри одного процесса consumer
один (Часть A, таблица 4.2) — значит один процесс может оставить **не более одного** claimed-и-не-
завершённого кадра в `found/` на момент своего краха. При нескольких одновременно работающих процессах
(файл 47, §4.5 явно рассматривает такой случай) это ограничение применяется **независимо к каждому
процессу** — то есть потенциально по одному такому кадру на каждый упавший процесс, не один суммарно.

**Оговорка к третьей строке таблицы — различие между фактом и знанием о факте (уточнено по ревью
владельца — раунд 5, не третье состояние операции).** На сервере результат списания всегда бинарен:
запрос либо был обработан и баланс изменён, либо нет — третьего физического исхода не существует. Если
процесс упал **после отправки** HTTP-запроса, но **до получения ответа**, локальный процесс после этого
падения не имеет способа узнать, какой из двух бинарных исходов произошёл на сервере — это отсутствие
информации у мёртвого процесса, а не отдельное состояние самой операции списания. Тот же класс
неопределённости уже принят Частью A-М для сетевых сбоев 1.0/2.0 («результат списания неизвестен») — Part
B не изобретает новую категорию, только констатирует, что она применима и здесь.

**Явное принятое ограничение, не тихий пробел:** Part B не вводит recovery/идемпотентность для `found/` —
осиротевшие кадры (по одному максимум на упавший процесс) остаются на диске в неопределённом относительно
Part B состоянии журнала/списания/публикации до тех пор, пока владелец не почистит их руками — тот же
режим, что уже принят для `errors/`.

### 4.1.2 🔴 Новый блокер (найдено ревью владельца) — producer пишет JPEG, пока consumer его сканирует

**Вопрос:** `producer` сохраняет кадр через `cv2.imwrite(path, frame)` напрямую в финальное имя
`NNNNNN.jpg` (C-06). `cv2.imwrite` — блокирующий вызов, но файловая система обычно делает имя файла
видимым в листинге каталога **раньше**, чем весь буфер записан и файл закрыт. Единая очередь (§4.1)
активно расширяет то, что́ и как часто сканирует consumer (весь `pending/*/*.jpg`, не только «старые»
файлы предыдущих сессий) — то есть именно тот момент, когда файл только что появился в текущей сессии,
чаще попадает в зону видимости consumer'а, чем при более узком (per-sid) скане.

**Проверено по Части A: контракта, исключающего это, НЕТ.** Единственное близкое место — таблица отказов
4.3 (файл 14), столбец E (`cv2.imwrite`): «возможен обрезанный файл **при заполнении диска** → consumer
получит битый JPEG → `errors/`» — это другая причина (нехватка места), не гонка по времени между двумя
потоками. Часть A нигде не описывает запись через временное имя + atomic rename (`os.replace`) для
producer'а — `cv2.imwrite` пишет сразу в целевое имя.

**Result: находка реальна.** Возможен сценарий: producer открыл `NNNNNN.jpg` и начал писать байты;
consumer (единая очередь, §4.1) в этот момент сканирует `pending/` и видит это же имя файла в листинге;
consumer пытается прочитать его для YOLO **до** того, как producer закончил запись. **Уточнение по ревью
владельца (раунд 5) — конкретный результат зависит от ОС/момента, не только «битый JPEG»:** на Windows
попытка `os.replace`/открытия файла, который producer в этот момент ещё держит открытым для записи, может
дать не «неполный JPEG», а `PermissionError`/`WinError 32` («файл занят другим процессом») — та же
находка, что уже отмечена в AB-4 (файл 45, Stage 5) для гонки TTL/consumer, только здесь источник —
producer, а не TTL. Оба симптома (неполный JPEG **или** `WinError 32`) — проявления одной причины (чтение
файла, который ещё не закончил писаться), не два независимых бага.

### Предлагаемое решение — проверено практически, не только в теории

**Технический нюанс, пойманный ревью владельца: `cv2.imwrite()` определяет формат по расширению имени
файла — `NNNNNN.jpg.tmp` не заканчивается на `.jpg`/`.jpeg`, и вызов падает.** Проверено эмпирически в
этой сессии:

```
>>> cv2.imwrite('frame.jpg.tmp', frame)
cv2.error: ... could not find a writer for the specified extension
```

**Исправленный механизм — `cv2.imencode` (кодирует в память, не привязан к имени файла) + запись байт +
`os.replace`:**

```python
try:
    ok, encoded = cv2.imencode('.jpg', frame)  # формат задаётся явно аргументом, не именем файла
except cv2.error:
    ...                                         # C-15 — imencode может бросить исключение, не только False
    return
if not ok:
    ...                                         # тот же путь, что и текущий False у imwrite (C-15, 4.3.A)
    return
tmp_path = pending_dir / f"{frame_id}.tmp"     # НЕ .jpg — не должен попадать в скан consumer'а/TTL
try:
    with open(tmp_path, 'wb') as f:
        f.write(encoded.tobytes())
except OSError:
    ...                                         # C-15, как и раньше для сбоя записи
    return
os.replace(tmp_path, pending_dir / f"{frame_id}.jpg")   # атомарный rename — тот же примитив, что C-08
```

Проверено практически (эта же сессия): `cv2.imencode('.jpg', frame)` → `imwrite`-эквивалентные байты →
запись в `.tmp` → `os.replace` в `.jpg` → `cv2.imread` читает результат корректно. Временное имя **не**
содержит `.jpg` вообще (`NNNNNN.tmp`, не `NNNNNN.jpg.tmp`) — важно не только для `cv2.imwrite` (который
здесь больше не используется), но и для того, чтобы consumer (сканирует `*.jpg`, §4.1) и TTL (тот же
скан, файл 46 §4.3) **структурно** не видели временный файл ни при каком раскладе, а не полагались на то,
что он не успеет появиться в листинге.

**🔒 Явный контракт (добавлено по ревью владельца, раунд 8 — недостающий контрольный инвариант, не
косметика):** `os.replace()` вызывается **только и исключительно** после того, как запись `.tmp`-файла
завершилась успешно (оба предыдущих шага — `cv2.imencode` и запись байт — вернули success). При исключении
`cv2.imencode()`, `ok == False`, или исключении при записи в `.tmp` — `os.replace()` для этого кадра **не
вызывается вообще**, обработка кадра прекращается на этом же шаге (`return` в псевдокоде выше уже это
отражает структурно; здесь это зафиксировано как явный инвариант, а не подразумевается по коду). Это
делает жизненный цикл строго бинарным: либо `encode success → write success → replace` (кадр появляется
в очереди как `.jpg`), либо обрыв на любом из первых двух шагов и `.jpg` не появляется вообще — среднего
состояния «частично опубликованный `.jpg`» не существует ни при каком порядке сбоев.

**Явно определённая судьба `.tmp`-файла (не оставлено на усмотрение реализации):**
- `cv2.imencode` бросил исключение или вернул `False`: `.tmp`-файл не создаётся вообще (до записи на диск
  дело не доходит) — тот же исход по последствиям, что сегодня даёт `False` от `cv2.imwrite` (C-15,
  таблица 4.3, столбец A). `os.replace()` не вызывается (см. контракт выше).
- Исключение при записи байт в `.tmp` (например диск заполнился): тот же путь C-15, что и сегодня для
  сбоя `cv2.imwrite` — `.tmp`-файл при этом может остаться недописанным на диске. `os.replace()` не
  вызывается.
- **Crash процесса между успешной записью `.tmp` и вызовом `os.replace`:** `.tmp`-файл остаётся на диске,
  `NNNNNN.jpg` не появляется вообще — **уточнение по ревью владельца (раунд 8):** точнее говорить не
  «кадр не существовал», а «кадр не стал элементом очереди» — producer его физически захватил (данные
  есть в `.tmp`), но `.jpg`, по которому его видят consumer/TTL, не появился, поэтому обработка через
  очередь для него не выполняется. Сам `.tmp`-файл при этом **не является частью recoverable-очереди**
  (ни consumer, ни TTL его не видят по построению, потому что оба сканируют `*.jpg`) — это чистый
  диагностический/мусорный остаток.
- **После успешного `os.replace()`:** исходный `.tmp`-путь по семантике `os.replace`/`rename` на этом же
  томе больше не существует (переименование, не копирование) — отдельной очистки `.tmp` после успешной
  публикации кадра не требуется, это не отдельный шаг lifecycle, а автоматическое следствие атомарного
  rename.

**Решение владельца, не доказанная техническая необходимость (уточнено по ревью — раунд 6):** для
`.tmp`-файлов сознательно не вводится отдельный числовой лимит/TTL — по аналогии с уже принятым режимом
`errors/`/`found/`-сирот (§4.1.1), ручная очистка. Это выбор, а не следствие того, что специальный
механизм технически невозможен или не нужен — если владелец захочет иное, это отдельное решение, не
пересмотр архитектуры.

**Почему это не смешивание с Частью A-М/чужим scope (в отличие от идемпотентности §4.1.1):** это чисто
файловая правка внутри producer'а, не денежная логика — по духу принадлежит именно конкурентности (Part
B), даже хотя технически меняет процедуру записи кадра, которая раньше была описана в Части A. Уместно
внести здесь, а не откладывать в Часть A-М.

---

## Что дальше (раунд 8, только этот файл — кластер D)

По ревью владельца (первый проход по уже разбитым файлам, отдельно от редактуры):
- §4.1.1 — исправлена внутренняя нестыковка «точки между завершёнными шагами» (третья строка таблицы
  находится ВНУТРИ незавершённого сетевого шага списания, не между двумя завершёнными шагами) — таблица
  и обе поясняющие фразы переформулированы;
- §4.1.2 — добавлен явный контракт: `os.replace()` вызывается только после успешной записи `.tmp`,
  при любом сбое `imencode`/записи — `os.replace()` не вызывается вообще (было структурно верно в
  псевдокоде через `return`, теперь зафиксировано как явный инвариант в прозе); добавлена судьба `.tmp`
  после успешного `os.replace()` (перестаёт существовать по семантике rename, отдельной очистки не
  требуется);
- «кадр просто не существовал» → «кадр не стал элементом очереди» (не путать факт захвата producer'ом с
  фактом попадания в очередь);
- §4.1 — «устраняет потерю бесплатно» заменено на точное описание (без нового recovery-механизма, но с
  явно найденной и закрытой ценой — producer/consumer race); упрощена аргументация про `mtime`/FIFO.

Читать вместе с [файлом 46](exchange-scout-part-b-design-46.md) (кластеры A/B/C) и
[файлом 47](exchange-scout-part-b-design-47.md) (кластер E, числа, итог) — три файла одного Design.
Ждёт ревью владельца по файлам 46/47 отдельно. Stage 5 не начат.
