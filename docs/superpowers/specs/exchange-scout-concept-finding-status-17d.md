# Концепт, часть 17d — Обработка найденного скриншота и статусы (разделы 7–8 входящих)

> Индекс концепта: [`exchange-scout-concept-17.md`](exchange-scout-concept-17.md). Части: [17a](exchange-scout-concept-calibration-17a.md) · [17b](exchange-scout-concept-snake-settings-17b.md) · [17c](exchange-scout-concept-queue-17c.md) · [17d](exchange-scout-concept-finding-status-17d.md) · [17e](exchange-scout-concept-compatibility-17e.md).

---

## Раздел 7. Обработка найденного скриншота биржи

**Замысел из входящих (по пунктам) и где закреплено:**

| Пункт входящих | Где закреплено | Комментарий |
|---|---|---|
| Увеличить область в левом нижнем углу (мини-карту); остальное изображение можно удалить или не передавать дальше | частично: кроп `position_crop_box=(0, 990, 300, 1080)` для OCR в конструкторе `ExchangeScoutEngine` (спека №3, часть 9); кадр переносится в `found/<sid>/` **целиком** (C-08) | **Расхождение:** входящие допускают удаление остальной части кадра, спека №3 сохраняет весь кадр. Решение владельца ниже |
| Сохранить существующий звуковой сигнал | спека №3, §4.6: `winsound.PlaySound` с запасным `Beep` | закреплено |
| Списывать 10 алмазов, как прежде | спека №2 (C-17, C-20; отдельный `hunt_type`, цена всегда 10◆) | закреплено |
| Затем запускать распознавание координат | спека №3 (C-11, C-12: OCR через `PositionReader`, `coords_ok`) | закреплено; порядок «OCR → журнал → списание» — спека №1, 4.0 |
| Обработка координат параллельно с передачей фрагмента в debug-бот | канал debug-Telegram отменён; вместо него публикация на сайте (спека №1, P-07: вне consumer-потока, без ожидания ответа) | решение владельца: Telegram не используется |
| В идеале отправлять в Telegram примерное расположение или координаты | публикация K/X/Y на сайте в разделе «Рой» (спека №1) | решение владельца |
| Не блокировать обработку остальных кадров из-за отправки | спека №1, P-07, P-08 | закреплено |
| Проверить, что повторное обнаружение одной биржи не ведёт к повторному списанию/дублированию уведомлений | решение владельца: дедупликация сейчас не нужна (спека №1, PA-4/PA-5; спека №2, C-20) | не реализуется; отдельная задача при необходимости |

**Решение владельца, которое нужно:** оставлять ли в `found/<sid>/` весь кадр (как в спеке №3, C-08) или только увеличенный кроп левого
нижнего угла; если только кроп — правка спеки №3 (C-08, размер на диске, аудит найденного вручную).

---

## Раздел 8. Завершение работы и статусы

**Замысел из входящих.** Когда змейка завершила обход всего королевства: кнопка старта/остановки переходит в **коричневое** состояние («идёт анализ
накопленных скриншотов»); после завершения обработки очереди — **зелёная**. Постоянно видимый небольшой индикатор статуса поверх остальных окон,
предпочтительно в правом нижнем углу, размером с небольшой значок рабочего стола, сообщает, что операция полностью завершена. Предложенные
состояния: зелёный мигающий — бот простаивает и готов; активное состояние — идёт обход или обработка; коричневый — обход завершён, нейросеть
анализирует очередь; зелёный стабильный — очередь обработана; отдельное состояние ошибки — сбой обработки, переполнение очереди, невозможность
отправить данные. Логика относится только к поиску бирж и не меняет другие режимы. Пока в очереди остаются скриншоты, обработка продолжается,
после опустошения останавливается.

**Что есть в коде (проверено).**
- Кнопка Старт: `self.start_button` (`main.py:2210-2216`), высота 56, цвет покоя `MD3["green_btn"]`. При запуске переключается на «Стоп» цветом
  `MD3["error"]` (`main.py:3604-3606`, `:3697-3700`), при остановке возвращается на зелёный (`:3619-3622`, `:3680-3683`); переключатель —
  `toggle_bot` (`main.py:3568`); зеркальная кнопка `_roy_hunt_btn` обновляется вместе с ней. Коричневого состояния нет.
- Постоянного индикатора статуса нет. Есть паттерн небольших окон поверх остальных: `tk.Toplevel` с `overrideredirect(True)` и
  `-topmost` — точка калибровки (`main.py:3473-3475`) и рамка сундуков (`:3510-3512`).
- Определения «обход королевства завершён» нет ни в `navigator.py`, `minimap_reader.py`, `engine.py` (поиск по признакам конца обхода пуст), ни в
  спеках №1–№3.

**Внутреннее противоречие входящих.** Раздел 6 говорит: змейка работает постоянно до `ESC`. Раздел 8 требует состояния «змейка завершила обход
всего королевства». Из текста не следует, как бот узнаёт, что обход завершён (граница карты? пройденная площадь? ручной стоп?). Пока это не
определено, состояния «обход завершён → коричневый» невыразимы в контрактах.

**Дополнительные неопределённости:**
1. Цвета: и «простой» (мигающий), и «очередь обработана» (стабильный) зелёные — различаются только миганием; цвет «активного» состояния и состояния
   ошибки не названы.
2. Размер индикатора («сопоставим с небольшим значком рабочего стола») — числа в пикселях не заданы.
3. Поведение индикатора при нескольких мониторах и в полноэкранной игре (окно поверх остальных может мешать кликам по игре).
4. Локализация подписей состояний (19 языков).
5. Как индикатор и кнопка ведут себя при `ESC` (аварийный стоп): что показывается, если очередь не пуста.

**Статус в спеках:** нет.

**Решения владельца, которые нужны до спеки:**
1. Что считается «обход королевства завершён» (или отказ от этого состояния в первой версии: остаются «идёт» / «анализ очереди» / «готово»).
2. Полная палитра состояний (цвета и мигание) и размер индикатора.
3. Нужен ли индикатор в первой версии или достаточно кнопки.
