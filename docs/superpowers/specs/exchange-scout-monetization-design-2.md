# Биржа 2.0 (Exchange Scout) — ЧАСТЬ A-М: монетизация — ИНДЕКС

> **Статус:** ЧЕРНОВИК. **Спека №2 из 3** — ждёт своей очереди на внешнее ревью (GPT, Gemini): не пройдено. К реализации НЕ готова. Общий индекс: [`exchange-scout-index-16.md`](exchange-scout-index-16.md).
> **Тип:** SPEC (не PLAN, не код). **Дата:** 2026-09-18.
> **Протокол:** `docs/РАБОТА-С-ДОКУМЕНТАМИ.md`. Стадии 1-7 и 9 пройдены.
> **Stage 8:** не запрошен владельцем (золотое правило 2026-09-18: агенты только с явного «да»); вместо него
> автор сам прошёл адверсариальный проход по коду (A-14…A-19).
> **Раунд правок: 5** (раунды 2–3 — закрытие ОВ-1, ОВ-3, снятие Telegram; 4–5 — замечания внешнего ревью GPT по спеке №2, проверены по коду). Лимит раундов отменён 2026-09-18: правим до одобрения GPT/Gemini.
> **Происхождение:** выделено из Части A по решению владельца от 2026-09-18.
> **Scope:** только деньги — списание 10◆ за находку Биржи 2.0. Публикация на сайт — отдельная спека:
> [`exchange-scout-roy-publication-design-1.md`](exchange-scout-roy-publication-design-1.md).

## Цепочка 2.0 (решение владельца)

**YOLO нашёл биржу → списать 10◆ → показать пользователю находку → обработать сохранённый скрин → получить
координаты → выложить находку в «Рой».** Стоимость всегда 10◆; других типов находок и других сумм в этом режиме нет.

## Из чего состоит документ

Файлы ≤300 строк, резали по границам стадий протокола.

| Файл | Что внутри |
|---|---|
| [`exchange-scout-monetization-context-4.md`](exchange-scout-monetization-context-4.md) | Стадии 1-2: цепочка списания 1.0 (2.7), серверные `/use_credit`, rate limit и long-poll баланса (2.8); 2.6 — `debug_reporter.py` (⛔ заменено публикацией на сайт) |
| [`exchange-scout-monetization-contracts-design-5.md`](exchange-scout-monetization-contracts-design-5.md) | Стадия 3: **C-17**, **C-20**, **C-21**; C-18/C-19 ⛔ заменены. Стадия 4: 4.3-М, 4.8, 4.9-М (снятие лимита для 2.0), 4.10 ⛔ заменена, 4.11 non-goals |
| [`exchange-scout-monetization-adversarial-6.md`](exchange-scout-monetization-adversarial-6.md) | Стадия 5: A-14…A-19 (A-17, A-18 ⛔ заменены) |
| [`exchange-scout-monetization-tests-gate-7.md`](exchange-scout-monetization-tests-gate-7.md) | Стадия 6: T-22…T-34 (T-25…T-27 ⛔ заменены); Стадии 7-9, открытый вопрос ОВ-2 |

Файловый цикл, GUI, `scout_results.jsonl` — [Часть A](exchange-scout-part-a-lifecycle-design-3.md).
Публикация на сайт (контракты P-01…P-13, тесты PT-01…PT-20) —
[спека публикации](exchange-scout-roy-publication-design-1.md).
Часть B (конкурентность) — не написана; переданные в неё места помечены **[→ Часть B]**.

## Закрытые решения владельца (не предмет обсуждения)

1. **Механизм оплаты 2.0 тот же, что в 1.0:** гейт баланса перед стартом и списание после находки. При этом
   **2.0 использует отдельный `hunt_type`** (`<SCOUT_HUNT_TYPE>`, C-20); списание выполняется именно с этим типом,
   а не с `"exchange"` от 1.0; стоимость всегда 10◆ (C-17).
2. **Ложные срабатывания YOLO приняты:** повторного `fresh_scan()` перед списанием у 2.0 нет; кредит за
   ложное срабатывание возвращается вручную.
3. **`spend_credit` вызывается из фонового consumer-потока**, а не из GUI-потока.
4. **ОВ-1 закрыт:** для 2.0 ограничение «не чаще одного списания за 2 секунды» снято за счёт отдельного
   `hunt_type` (C-20); Биржа 1.0, её `hunt_type`, стоимость и защита не меняются. Никаких sleep, дедупликации и
   новых механизмов защиты сейчас не вводится.
5. **ОВ-3 закрыт, оформлен контрактом C-21:** находки 2.0 учитываются в статистике отдельной категорией «Биржа 2.0» по своему `hunt_type`, счётчики 1.0 не меняются (тесты T-29…T-32); имя типа — деталь плана (`<SCOUT_HUNT_TYPE>`, ≤20 символов).
6. **Telegram для 2.0 не используется** — находки выкладываются на сайт (спека публикации).

## Поведение после старта: что делает consumer, когда деньги кончаются

Гейт баланса проверяет только момент старта; каждая находка проверяется самим списанием (A-14). Публикация в «Рой» и показ как оплаченной — только после `success` (C-17(e), P-01 спеки №1).

| Ситуация | Consumer | Находка | Сессия | Публикация |
|---|---|---|---|---|
| `credits <= 0` на старте (C-17a) | сессия не создаётся | — | не стартует | — |
| `success` | счётчик обновлён; показ, обработка | оплачена | идёт | да (после OCR, P-01) |
| 402: баланс < 10◆ на очередной находке (в т. ч. 1–9◆ при пройденном гейте) | флаг остановки синхронно в consumer'е (A-14), страница пополнения | в журнале, НЕ оплачена, не показана как оплаченная | → `STOPPING` | нет |
| 403 / 404 | списания не было (`server/main.py:315-340`: атомарный `UPDATE` затронул 0 строк); ничего не показывается (ОВ-2 — не менять) | в журнале, не оплачена | идёт | нет |
| сетевой сбой / потерянный ответ, связь < 120 с | повтора нет (A-16); оплата неизвестна | в журнале; результат списания **неизвестен**; для pipeline не подтверждённо оплачена; не показывается как оплаченная | идёт | нет |
| сетевой сбой, связь > 120 с | `offline_stopped` | в журнале | → `STOPPING` | нет |

**Инвариант C-17(f):** после перехода в `STOPPING` ни одна находка, у которой оплата ещё не начата, не инициирует `spend_credit`; уже начатый вызов завершается и его исход применяется (тест T-34). Как именно останавливается / дренируется очередь — **[→ Часть B]**. Необработанные кадры очереди после остановки остаются на диске, не обрабатываются и не оплачиваются **[→ Часть A/B]**.

## Открытые вопросы владельцу

Нет. **ОВ-2 закрыт владельцем 2026-09-18:** тихие ветки 403/404 при списании **не менять** (существующее поведение 1.0 наследуется 2.0 как есть). Остаточные риски (флуд, повторы, списания за ложные срабатывания YOLO) приняты; дедупликация и лимит не добавляются.

---

## Что осталось непроверенным

1. **Какая ревизия серверного кода развёрнута на GCP** — соответствие прода репозиторию не проверялось.
2. **Число uvicorn-worker'ов на проде** — для 2.0 после C-20 не критично (лимита нет), для 1.0 не менялось.
3. **Фактическая частота ложных срабатываний YOLO на кадрах 2.0** (A-15) — измеряется только живым прогоном.
4. **Поведение `/use_credit` при потерянном сетевом ответе** разобрано по коду (A-16): long-poll `vault.py` возвращает актуальный баланс на каждом цикле (≤ ≈50 с), то есть выравнивается БАЛАНС на экране; факт оплаты конкретной находки клиент так и не узнаёт (результат списания неизвестен; для pipeline находка не подтверждённо оплачена и не публикуется; гарантировать, что кредит не списан, система не может). Живым вызовом не проверялось.
5. **Одновременные запросы двух процессов с одним HWID** — разобрано по коду (атомарный UPDATE), живым
   параллельным вызовом не проверялось.
