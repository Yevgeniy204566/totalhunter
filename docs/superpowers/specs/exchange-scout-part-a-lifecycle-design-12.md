# Биржа 2.0 (Exchange Scout) — ЧАСТЬ A: файловый жизненный цикл, контракты, GUI-интеграция — ИНДЕКС

> **Статус:** ✅ **Final Gate пройден 2026-09-19** (спека №3 из 3, раунды 5-10 внешнего ревью GPT/Gemini
> файлов 14/15/16 закрыты). К реализации (PLAN) готова **с оговоркой**: A-3 (гонка TTL/`os.replace`), A-4
> (единоличная обработка кадра между процессами) и A-12 (`active_mode` vs физическое завершение consumer'а)
> — обязательные незакрытые зависимости ещё не написанной Части B, не факультативные улучшения; PLAN по
> Части A может начинаться, но полная реализация 2.0 (с конкурентностью) ждёт Части B. Общий индекс:
> [`exchange-scout-index-17.md`](exchange-scout-index-17.md).
> **Тип:** SPEC (не PLAN, не код).
> **Протокол:** `docs/РАБОТА-С-ДОКУМЕНТАМИ.md`. Стадии 1-7 и 9 были пройдены и перепройдены в раунде 2 для
> предыдущей редакции; после решения владельца от 2026-09-19 (см. «Пересмотр по концепту» ниже) те
> результаты раунда 2 перестали закрывать затронутые lifecycle-контракты (C-02/C-03/C-08 и связанные T/A) —
> новая редакция (раунд 4, инвариант конвейера) прошла все стадии заново в раундах 5-10 (файлы 14/15/16 по
> отдельности) и Final Gate по ней пройден 2026-09-19 (см. статус выше).
> Stage 8 не запрошен владельцем (золотое правило 2026-09-18) — не блокирует Final Gate (файл 16, Стадия 8).
> **Дата:** содержание — 2026-09-18 (имя файла содержит 2026-09-17: задано в исходной постановке задачи,
> менять не стал); разделение — 2026-09-18.
> **Раунд правок: 10, Final Gate пройден** (раунд 3 = РАЗДЕЛЕНИЕ — в рамках самого разделения содержательных
> изменений контрактов Части A не вносилось: ни один контракт C-01…C-16, ни одна находка A-1…A-13, ни один
> тест T-01…T-21 не переписан по существу). **Раунд 4 (2026-09-19) — переработка по концепту:** C-02, C-03,
> C-08, 4.1, 4.2, non-goal 6 (файл 14), A-3/A-4/A-5/A-6 (файл 15), T-01/T-02/T-03/T-06/T-08 + Self-Audit +
> State Matrix (файл 16) переписаны под инвариант конвейера. **Раунды 5-10 (2026-09-19)** — внешнее ревью
> переработки раунда 4 (GPT/Gemini, файлы 14/15/16 по отдельности) последовательно нашло и закрыло: границы
> `stop()`/`active_mode`/`start()`-atomicity (C-04, C-05, C-13, C-14 ↔ A-12), сид-резервирование (C-01),
> TTL-семантику «право на удаление, не дедлайн» (C-02), новую реальную гонку TTL↔consumer (`os.replace` vs
> TTL-удаление, A-3 третий обход) — зафиксирована как открытый вход для Части B, не остаточный риск —
> и три самодельных дефекта тестовой матрицы (T-02 требовал неопределённого TTL-интерфейса; T-08 и T-09
> формулировали более сильные гарантии, чем даёт C-02/A-3). **Final Gate по текущей редакции пройден
> 2026-09-19** — с явной оговоркой, что A-3/A-4/A-12 остаются обязательными зависимостями Части B, не
> закрытыми контрактами.
> **Scope Части A:** файловый жизненный цикл сессии, контракты переиспользования существующих
> компонентов, GUI-каркас, формат `scout_results.jsonl`.

---
## Пересмотр по концепту (2026-09-19)

Решения владельца по документу «Входящие» (концепт `exchange-scout-concept-18.md`) меняют жизненный цикл кадров: **Инвариант конвейера (решение владельца 2026-09-19, концепт, файлы 21/22):** кадр не удаляется остановкой змейки (`ESC`/Стоп), остановкой нейросети, автопаузой и запуском/возобновлением. Кадр удаляется только (1) consumer'ом после обработки в порядке «обработка → передача результата → удаление кадра» либо (2) ботом по истечении 30 минут с момента создания скриншота, если он не обработан. `pending/` не очищается при старте и возобновлении; остатки очереди обрабатываются или устаревают. `ESC` останавливает змейку, но не consumer; отдельная кнопка останавливает нейросеть. Устройство очереди и папок при перезапуске (по `sid` или единая очередь) определяет Часть B. Под пересмотр попадали: **C-02, C-03, C-08**, тесты **T-01, T-02, T-03, T-08**, находки **A-6** и «никогда не выбрасывать», раздел 4.1/4.2, non-goal 6. **Переработка выполнена 2026-09-19** (раунд 4, файлы 14/15/16); вопросы конкурентности и межпроцессных гарантий, выявленные при этом, переданы в Часть B (ещё не написана). Ждёт внешнего ревью, до его прохождения Final Gate по новой редакции не считается пройденным.


## Из чего состоит документ

Документ разрезан на файлы по решению владельца (2026-09-18: ни один файл спеки не длиннее ~300 строк).
Резали по границам стадий протокола, а не по счётчику строк; содержание не сокращалось и не переписывалось.

| Файл | Что внутри |
|---|---|
| [`exchange-scout-part-a-context-13.md`](exchange-scout-part-a-context-13.md) | Стадия 1 (Context), Стадия 2 (Code Archaeology): `PositionReader`, `CoastalSnakeNavigator`, `PacmanEngine`, GUI `main.py`, честная фиксация отсутствия прямого аналога файлового цикла |
| [`exchange-scout-part-a-contracts-design-14.md`](exchange-scout-part-a-contracts-design-14.md) | Стадия 3 (контракты **C-01…C-16**), Стадия 4 (структура на диске, владение ресурсами, отказы внешних вызовов, сигнатура `ExchangeScoutEngine`, переиспользование компонентов, схема `scout_results.jsonl`, GUI-интеграция, non-goals) |
| [`exchange-scout-part-a-adversarial-15.md`](exchange-scout-part-a-adversarial-15.md) | Стадия 5 (Adversarial Review, находки **A-1…A-13**) |
| [`exchange-scout-part-a-tests-gate-16.md`](exchange-scout-part-a-tests-gate-16.md) | Стадия 6 (тесты **T-01…T-21**), Стадия 7 (Self-Audit + state matrix), Стадия 8 (External Agent Review), Стадия 9 (Final Gate) |

**Спека монетизации — отдельный документ, [→ Часть A-М]:**
[`exchange-scout-monetization-design-07.md`](exchange-scout-monetization-design-07.md)
(кредиты 2.0 — C-17; debug-Telegram — C-18, C-19; находки A-14…A-19; тесты T-22…T-34).

**Часть B (конкурентность: очередь, треды, graceful shutdown, race conditions)** — ещё не написана.
Места, переданные в неё, помечены в тексте как **[→ Часть B]**.

## Где что искать по номерам

| Номера | Где |
|---|---|
| C-01…C-16 | файл 14 |
| C-17, C-18, C-19 | Часть A-М (в Части A эти номера не переиспользуются) |
| A-1…A-13 | файл 15 |
| A-14…A-19 | Часть A-М |
| T-01…T-21 | файл 16 |
| T-22…T-34 | Часть A-М; файл тестов при этом общий — `test_exchange_scout_lifecycle.py` (клиентские T-22…T-27, T-33, T-34) / `server/tests/` (серверные T-28…T-32) |
| Секции 2.6, 2.7, 4.8, 4.10 | Часть A-М; в файлах 13 и 14 на их месте оставлены заглушки-ссылки |

Нумерация при нарезке и при выносе монетизации **не сдвигалась** — именно для того, чтобы ссылки из
Части A-М и из будущей Части B не сломались.

## История раундов

- **Раунд 1** — первый черновик: файловый цикл, контракты C-01…C-16, находки A-1…A-13, тесты T-01…T-21.
  Stage 8 пропущен осознанно, с фиксацией проверенных условий.
- **Раунд 2** — два решения владельца: монетизация закрыта контрактом C-17; каждая находка дублируется в
  debug-Telegram (C-18, C-19). На этом этапе добавлены A-14…A-18, T-22…T-27; A-19 и T-28…T-34 добавлены
  позже, уже в самостоятельной Части A-М, после её выделения в раунде 3. Пропуск Stage 8 отменён (деньги в scope).
  Достигнут лимит раундов протокола → остановка через `AskUserQuestion`.
- **Раунд 3** — разделение по решению владельца: деньги и Telegram вынесены в Часть A-М, документ нарезан
  на файлы ≤300 строк. Контракты Части A не менялись; решение владельца от 2026-09-19 выявило отдельный
  lifecycle-пересмотр (C-02/C-03/C-08 и связанные T/A), ещё не внесённый в эти файлы.
- **Раунд 4 (2026-09-19)** — переработка по инварианту конвейера: C-02, C-03, C-08, разделы 4.1/4.2,
  non-goal 6 (файл 14), находки A-3/A-4/A-5/A-6 (файл 15), тесты T-01/T-02/T-03/T-06/T-08 + Self-Audit +
  State Matrix (файл 16) переписаны и перекрёстно сверены. Stage 8 унифицирован под золотое правило
  2026-09-18. JPEG `quality=95` подтверждён владельцем. Риск роста `pending/` при остановленной нейросети
  (Gemini) зафиксирован как открытый вход для Части B, не принят как остаточный. Внешнее ревью GPT/Gemini
  переработки прошло несколько итераций правок файла 14 (три коммита) внутри этого раунда: сид-резервирование
  (C-01), TTL-семантика «право на удаление, не дедлайн» (C-02), границы `stop()`/`active_mode` (C-05, C-13,
  C-14 ↔ A-12) и терминологические уточнения.
- **Раунд 5 (2026-09-19)** — очередная итерация внешнего ревью нашла новую реальную гонку: TTL может
  удалить кадр между тем, как consumer уже решил его перенести (`os.replace`, C-08), и самим переносом —
  `OSError` от этой легитимной гонки сейчас неотличим от `OSError` настоящего сбоя ФС и одинаково трактуется
  C-15 как критический стоп сессии. Зафиксировано как открытый вход для Части B (A-3 третий обход, файл 15;
  C-08/C-15, файл 14; см. «Открытый архитектурный вход для Части B» ниже) — не принято как остаточный риск.
  Также добавлена перекрёстная ссылка C-03 → A-6 (failure path ручного удаления `found/` во время обработки
  уже был определён, но не был виден из C-03) и мелкие уточнения (4.4 п.4 структурно разделён, `errors/`
  уточнён как «без роста за сессию», а не «без роста вообще»).

## Открытые вопросы владельцу (по Части A)

1. ~~**Качество JPEG 95**~~ — **ЗАКРЫТ владельцем 2026-09-19:** `IMWRITE_JPEG_QUALITY = 95` подтверждён
   как окончательный дефолт (файл 14, секция 4.1).
2. **A-12 и A-4** (файл 15) — взаимное исключение режимов и «два процесса бота» закрыты лишь частично;
   полное закрытие передано в Часть B как обязательное.
3. ~~**Запускать ли Stage 8 для файловой Части A**~~ — **ЗАКРЫТ унификацией под золотое правило
   2026-09-19:** Stage 8 не запрошен владельцем и не является блокирующим условием Final Gate (файл 16,
   Стадия 8).

Денежные открытые вопросы (ОВ-1 про HTTP 429, ОВ-2 про тихие ветки 403/404) — **[→ Часть A-М]**.

---

## Что осталось непроверенным

Явный список, а не растворённые в тексте оговорки.

1. **Эквивалентность детекции «живой кадр vs JPEG q95»** (A-10) — не доказана, проверяется только живым
   прогоном YOLO на сохранённых кадрах.
2. **Подача каналов в `PositionReader` на историческом кадре** (A-11, C-11) — рассуждением не
   закрывается; закрывается тестом T-21 на реальном сохранённом кадре, который ещё предстоит написать.
3. **Поведение при двух процессах бота одновременно** (A-4) — эксклюзивность между процессами не
   гарантирована; lock-файл как кандидат передан в Часть B и живым запуском двух процессов не проверялся.
4. **Физическое завершение тредов после `stop()`** (A-12, C-05) — Часть A даёт гарантию только по
   состоянию на диске; момент фактического завершения тредов — Часть B.
5. **Реальный объём сессии на диске** (файл 14, секция 4.1) — оценка 80-140 МБ на «прикидочные 200 кадров»
   взята из арифметики q95, а не из замера; после инварианта конвейера (кадр не удаляется при остановке
   NN) эта оценка не учитывает рост очереди при выключенной нейросети — см. «Открытый архитектурный вход
   для Части B» ниже.
6. **Новый lifecycle-инвариант удаления кадров** (решение владельца 2026-09-19) — C-02/C-03/C-08, тесты
   T-01/T-02/T-03/T-06/T-08 и связанные находка A-3/A-4/A-5/A-6, разделы 4.1/4.2 и non-goal 6
   **переработаны 2026-09-19 (раунд 4)** в файлах 14/15/16, прошли внешнее ревью GPT/Gemini (раунды 5-10) и
   перекрёстно сверены друг с другом. **Final Gate пройден 2026-09-19** — не блокирует переход к PLAN. A-3
   (гонка TTL/`os.replace`), A-4 (единоличная обработка кадра) и A-12 (`active_mode` vs физическое
   завершение consumer'а) остаются обязательными незакрытыми зависимостями Части B (см. ниже), но это не
   блокирует PLAN по самой Части A.
7. Денежные и Telegram-непроверенные пункты — **[→ Часть A-М]**, раздел «Что осталось непроверенным».

---

## Открытый архитектурный вход для Части B

**Неограниченный рост `pending/` при остановленной нейросети** (найдено Gemini 2026-09-19, признано
владельцем реальным риском, **не принято как остаточный** — в отличие от A-5, где неопределённость объёма
за одно TTL-окно уже принята). TTL (C-02) делает необработанный кадр старше 30 минут подлежащим удалению
(не гарантирует фактическое удаление — см. файл 14, 4.2), но независимое от NN
выполнение TTL-прохода ещё не спроектировано (**[→ Часть B]**): если TTL-проход окажется завязан на тот же
механизм, что и NN, то при выключенной отдельной кнопкой нейросети TTL тоже не будет выполняться, пока NN
не включат обратно — тогда змейка продолжает писать кадры без гарантированного верхнего предела по объёму,
поскольку независимое выполнение TTL ещё не определено. Это не «непроверенный факт» (список выше), а
**обязательный вход в дизайн Части B**: решение владельца 2026-09-19 — сейчас не выбирать ни конкретный
механизм, ни число (независимый от NN таймер TTL, жёсткий лимит с backpressure на producer и другие
варианты остаются открытыми до проектирования Части B); пункт не закрывается молчаливым принятием риска и
должен быть явно решён при написании Части B, не отложен бессрочно.

**Гонка TTL против consumer'а на одном и том же кадре** (найдено внешним ревью, раунд 5, 2026-09-19,
файл 15 A-3 третий обход). Если TTL-проход выполняется отдельно от consumer'а, он может удалить кадр,
которому уже перевалило за 30 минут, ровно в тот момент, когда consumer уже решил перенести этот же кадр
(YOLO нашёл совпадение) и вызывает `os.replace()` (C-08). Это два независимо корректных действия
(consumer следует C-08, TTL следует C-02), но их столкновение по текущему тексту C-15 трактуется как
**критический сбой всей сессии** — то есть штатное поведение TTL может выглядеть как поломка pipeline и
останавливать сканирование. Часть A **не различает** «`OSError` от реального сбоя ФС/прав» и «`OSError`
от этой гонки»; различение (по типу исключения, сериализация TTL и consumer'а, либо иной подход) —
**обязательный вход в дизайн Части B**, не закрывается молчаливым принятием текущей (излишне грубой)
реакции.
