# Калибровка и рекомендуемые значения — Стадия 3 (Contract Extraction)

> Индекс и статус: [`exchange-scout-calibration-context-32.md`](exchange-scout-calibration-context-32.md).
> Части: [32](exchange-scout-calibration-context-32.md) (Context) ·
> [33](exchange-scout-calibration-archaeology-33.md) (Code Archaeology) · **34 (этот файл)**.
> Контракты — только для **Раздела 1** (единая панель, 12 целей калибровки). Раздел 2 (рекомендуемые значения)
> остаётся заблокирован тремя открытыми продуктовыми вопросами из файла 32 — контрактов по нему здесь нет,
> это не пробел, а прямое следствие того, что Contract Extraction не может изобретать данные владельца.

---

## 3.1 Контракты Раздела 1 — единая панель калибровки (12 целей)

Формат: `Requirement → Contract/Invariant → Enforcement (планируемый) → Verification`.

| # | Requirement | Contract / Invariant | Enforcement (планируемый) | Verification |
|---|---|---|---|---|
| C-01 | Один вход вместо двух несвязанных | Сегодня — два раздельных пути (отдельная кнопка калибровки A/Б, сворачивающая окно; выпадающий список тюнинга, инлайн). Панель обязана свести оба в **один** пользовательский путь: единый список/навигация на 12 целей, без скрытого второго входа | Design выбирает конкретный виджет (список, вкладки внутри вкладки и т. п.); контракт фиксирует только «один вход», не форму | Ручная проверка UI / GUI-тест на отсутствие второго независимого триггера калибровки A/Б |
| C-02 | Тип цели — явное поле данных, не структура кода | Каждая из 12 целей имеет явный тип ∈ {точка, OCR-область, смещение-к-YOLO} (файл 33, находка 2.2). Тип **не выводится** из того, в какой из трёх сегодняшних структур (`_TUNE_POINT_TARGETS`/`_CHEST_TUNE_RECTS`/нигде) цель перечислена — это данные, которые единый источник метаданных обязан хранить явно | Единый источник метаданных 12 целей с явным полем `kind`; конкретная структура хранения (таблица, реестр, что-то ещё) определяется на Design, не здесь | Тест проходит по всем 12 целям и проверяет, что `kind` задан и принадлежит фиксированному множеству |
| C-03 | Единая отрисовка маркера на тип, не три реализации на всё | Сегодня три независимые функции рисуют концептуально одно и то же («покажи, где точка»): `calibration_ui._show_red_dot` (только А/Б), `main._show_crosshair_at` (уже переиспользуется между джойстик-точкой и превью тюнинга — файл 33, 2.3), `main._show_chest_rect_overlay` (только области). Панель обязана рисовать маркер **одним путём на тип** (один рендерер для «точка», один для «область», явное состояние для «смещение-к-YOLO»), не тремя историческими путями | `_show_red_dot` выводится из употребления, А/Б рисуются тем же путём, что и точки тюнинга; область — отдельный, но единственный путь; для «смещение-к-YOLO» — явное состояние UI («статичное превью недоступно, позиция определяется в реальном времени»), не молчаливое отсутствие | Тест/ревью кода на этапе PLAN: не более одной функции отрисовки маркера на каждый тип |
| C-04 | Тематическая группировка и тип — независимые визуальные каналы | Сегодня `chest_collect` (тип «точка») визуально сгруппирован по цвету с `chest_sender`/`chest_type` (тип «область») только потому, что все три — «про сундуки» (`TUNE_CHEST_GROUP`, файл 33, 2.2/2.4). Единая панель обязана сохранять возможность такой тематической группировки (сундуки — не единственная будущая тема), но **тип цели должен быть определим по любой цели независимо от её тематической группы** — группировка по теме не должна маскировать различие точка/область/смещение-к-YOLO | Design выбирает конкретное визуальное решение (иконка типа, форма маркера, отдельная колонка и т. п.) — контракт фиксирует независимость каналов, не конкретный визуальный приём | Ручная проверка: для каждой из 12 целей тип считывается с экрана без обращения к тематической группе |
| C-05 | Единый стиль не переопределяет существующую координатную модель | Панель не меняет существующую модель хранения координат и офсетов: `coord_manager.calibrate()` (абсолютные координаты A/Б) и `coord_manager.set_ui_offset()` (смещения 10 целей тюнинга) сохраняют прежнюю семантику (файл 33, 2.1). Новый слой метаданных из C-02 (явный тип цели для единого UI) описывает **цели**, а не координаты — это не третий формат хранения координат и не замена существующих значений `coord_manager` | Единая панель вызывает существующие `calibrate()`/`set_ui_offset()` как есть; метаданные C-02 живут отдельно от координатных данных и не изменяют их формат | Тест: после ввода через новую панель значения в `coord_manager` идентичны тем, что дал бы старый раздельный UI при тех же введённых числах |
| C-06 | Видео — гиперссылка, не плеер | Раздел содержит ссылку на YouTube-ролик (URL будет предоставлен позже); видео не встраивается в интерфейс бота | Обычный текстовый/кликабельный элемент со ссылкой | Ручная проверка наличия ссылки и отсутствия встроенного видеоплеера |

## 3.2 Явные non-goals

1. **Порядок отображения 12 целей в списке** — сознательно не решается здесь (Context, файл 32, «Что НЕ
   решено», п.1). Design может выбрать любой порядок, контракты C-01…C-06 от этого не зависят.
2. **Конкретная визуальная реализация типов** (иконки, цвета, форма маркера для «смещение-к-YOLO») —
   Design, не Contract Extraction (Context, п.5 «Что НЕ решено»).
3. **Математика/логика существующих точек и офсетов не меняется** — уже зафиксировано входящими и
   подтверждено C-05.
4. **Раздел 2 (рекомендуемые значения) контрактов не имеет** — три открытых вопроса (список ползунков,
   источник диапазонов, форма UI) требуют ответа владельца, Contract Extraction не производит их из кода.

## 3.3 Зависимости между контрактами

```
C-02 (тип как данные) ──> обязательный вход для ──> C-03 (единая отрисовка на тип)
C-02 ──> обязательный вход для ──> C-04 (независимость каналов группировка/тип)
C-01 (один вход) ──> не зависит от C-02…C-06, но их проще выполнить, если единый источник метаданных
   из C-02 существует и используется тем же единственным входом
```

Конфликтов между контрактами не найдено. C-05 ограничивает все остальные: что бы ни выбрал Design для
UI (C-01, C-03, C-04), это не должно потребовать изменения формата данных `coord_manager`.

## 3.4 Что дальше

Design (Stage 4) для Раздела 1 может начинаться после одобрения владельцем этого файла — здесь нет
зависимостей от ещё не написанной Части B или GUI-спеки Биржи 2.0 (подтверждено уже в Context). Раздел 2
остаётся заблокированным до получения от владельца: списка ползунков, источника диапазонов, формы
интерфейса.
