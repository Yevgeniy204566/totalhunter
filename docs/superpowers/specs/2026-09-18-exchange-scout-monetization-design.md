# Биржа 2.0 (Exchange Scout) — ЧАСТЬ A-М: монетизация — ИНДЕКС

> **Статус:** ОДОБРЕНА владельцем 2026-09-18 (Stage 10 пройден).
> **Тип:** SPEC (не PLAN, не код). **Дата:** 2026-09-18.
> **Протокол:** `docs/РАБОТА-С-ДОКУМЕНТАМИ.md`. Стадии 1-7 и 9 пройдены.
> **Stage 8:** не запрошен владельцем (золотое правило 2026-09-18: агенты только с явного «да»); вместо него
> автор сам прошёл адверсариальный проход по коду (A-14…A-19).
> **Раунд правок: 3** (раунд 2 — закрытие ОВ-1; раунд 3 — снятие Telegram, закрытие ОВ-3, индекс; продолжение
> осознанное по решению владельца через `AskUserQuestion` 2026-09-18). Дальше — снова `AskUserQuestion`.
> **Происхождение:** выделено из Части A по решению владельца от 2026-09-18.
> **Scope:** только деньги — списание 10◆ за находку Биржи 2.0. Публикация на сайт — отдельная спека:
> [`2026-09-18-exchange-scout-roy-publication-design.md`](2026-09-18-exchange-scout-roy-publication-design.md).

## Цепочка 2.0 (решение владельца)

**YOLO нашёл биржу → списать 10◆ → показать пользователю находку → обработать сохранённый скрин → получить
координаты → выложить находку в «Рой».** Стоимость всегда 10◆; других типов находок и других сумм в этом режиме нет.

## Из чего состоит документ

Файлы ≤300 строк, резали по границам стадий протокола.

| Файл | Что внутри |
|---|---|
| [`…-monetization-01-context.md`](2026-09-18-exchange-scout-monetization-01-context.md) | Стадии 1-2: цепочка списания 1.0 (2.7), серверные `/use_credit`, rate limit и long-poll баланса (2.8); 2.6 — `debug_reporter.py` (⛔ заменено публикацией на сайт) |
| [`…-monetization-02-contracts-design.md`](2026-09-18-exchange-scout-monetization-02-contracts-design.md) | Стадия 3: **C-17**, **C-20**; C-18/C-19 ⛔ заменены. Стадия 4: 4.3-М, 4.8, 4.9-М (снятие лимита для 2.0), 4.10 ⛔ заменена, 4.11 non-goals |
| [`…-monetization-03-adversarial.md`](2026-09-18-exchange-scout-monetization-03-adversarial.md) | Стадия 5: A-14…A-19 (A-17, A-18 ⛔ заменены) |
| [`…-monetization-04-tests-gate.md`](2026-09-18-exchange-scout-monetization-04-tests-gate.md) | Стадия 6: T-22…T-28 (T-25…T-27 ⛔ заменены); Стадии 7-9, открытый вопрос ОВ-2 |

Файловый цикл, GUI, `scout_results.jsonl` — [Часть A](2026-09-17-exchange-scout-part-a-lifecycle-design.md).
Публикация на сайт (контракты P-01…P-13, тесты PT-01…PT-16) —
[спека публикации](2026-09-18-exchange-scout-roy-publication-design.md).
Часть B (конкурентность) — не написана; переданные в неё места помечены **[→ Часть B]**.

## Закрытые решения владельца (не предмет обсуждения)

1. **Механизм оплаты 2.0 тот же, что в 1.0:** гейт баланса перед стартом и списание после находки. При этом
   **2.0 использует отдельный `hunt_type`** (`<SCOUT_HUNT_TYPE>`, C-20); списание выполняется именно с этим типом,
   а не с `"exchange"` от 1.0; стоимость всегда 10◆ (C-17).
2. **Ложные срабатывания YOLO приняты:** повторного `fresh_scan()` перед списанием у 2.0 нет; кредит за
   ложную находку возвращается вручную.
3. **`spend_credit` вызывается из фонового consumer-потока**, а не из GUI-потока.
4. **ОВ-1 закрыт:** для 2.0 ограничение «не чаще одного списания за 2 секунды» снято за счёт отдельного
   `hunt_type` (C-20); Биржа 1.0, её `hunt_type`, стоимость и защита не меняются. Никаких sleep, дедупликации и
   новых механизмов защиты сейчас не вводится.
5. **ОВ-3 закрыт:** находки 2.0 учитываются в статистике отдельной категорией «Биржа 2.0», счётчики 1.0 не
   меняются; имя типа — деталь плана (`<SCOUT_HUNT_TYPE>`, ≤20 символов).
6. **Telegram для 2.0 не используется** — находки выкладываются на сайт (спека публикации).

## Открытые вопросы владельцу

Нет. **ОВ-2 закрыт владельцем 2026-09-18:** тихие ветки 403/404 при списании **не менять** (существующее поведение 1.0 наследуется 2.0 как есть). Остаточные риски (флуд, повторы, ложные списания) приняты; дедупликация и лимит не добавляются.

---

## Что осталось непроверенным

1. **Какая ревизия серверного кода развёрнута на GCP** — соответствие прода репозиторию не проверялось.
2. **Число uvicorn-worker'ов на проде** — для 2.0 после C-20 не критично (лимита нет), для 1.0 не менялось.
3. **Фактическая частота ложных срабатываний YOLO на кадрах 2.0** (A-15) — измеряется только живым прогоном.
4. **Поведение `/use_credit` при потерянном сетевом ответе** разобрано по коду (A-16: `vault.py` возвращает
   актуальный баланс на каждом цикле long-poll, расхождение самоустраняется ≈50 с), живым вызовом не
   проверялось.
5. **Одновременные запросы двух процессов с одним HWID** — разобрано по коду (атомарный UPDATE), живым
   параллельным вызовом не проверялось.
